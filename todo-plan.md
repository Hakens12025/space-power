# todo-plan.md —— 统一待办计划

> 建于 2026-09-19。这份文件收**还没做、要做、或者等拍板**的事,按系统分节。已经做完的事不放这儿(它们在各自的 `CLAUDE.md` 备忘、提交记录、或演示页的注释里)。
> 光速延迟有它自己的一份:`光速延迟接入计划.md`(只放光速延迟)。数值统一的证据与规范在 `态势感知的问题.md`;另一个 agent 的宪章草案是 `尺度预算.md`。
> 演示页在 `demos/` 下按系统分子目录(见第 6 节)。

| 节 | 系统 | 状态 |
|---|---|---|
| 1 | 战争迷雾 / 态势感知:玩法框架 | 方向已定(好玩第一),条目按 1.11 的优先级表推进 |
| 2 | 战争迷雾 / 态势感知:落地待办 | 演示页 `demos/sensors/态势感知V3.html` 是活的;引擎还是 SN4 |
| 3 | 武器系统:机动-武器 | **WR1 已做**(2026-09-22):射程无限、只是精准度问题 —— 主炮高斯角散布(σ=0.0081:九成 @15 万 / 五成 @36.6 万 / 一成 @196 万)、瞄接触的估计位置、火控门降到跟踪级;导弹去发射门、射程 = 燃料(37.5 万动力射程)、数据链引导瞄估计位置。细节与整局模拟见 `js/weapons/CLAUDE.md` 的 WR1 节。**待查**:bot 在 94 万公里开火(命中率门似乎没挡住)、蓝红导弹命中不对称 |
| 4 | 尺度落引擎 | 等第 2 节的感知落地一起做 |
| 5 | 与以上都无关的代码清理 | 等拍板 |
| 6 | 演示页与文档整理 | 2026-09-19 已归档进 `demos/` |

## 1. 战争迷雾 / 态势感知:玩法框架(2026-09-14 起;2026-09-19 从 `光速延迟接入计划.md` 第 11 节迁入)

> 原文照搬,只改了小节编号(1.N = 原 11.N;原 11.7"光速延迟在游戏里该占什么位置"留在那份文件里)。
> 文中的"第 N 节""8.x""9 节第 8 条"指的都是 `光速延迟接入计划.md`;"11.N"请读成本节的 1.N。

用户提出的要求是:这是游戏不是物理仿真,过于真实反而损失游戏性,要从游戏角度重想一遍。本节是那一轮的结论,记录下来备查。第 1 到 10 节回答"光速延迟怎么接进去",本节回答"整套迷雾该做成什么样,光速延迟在里面排第几"。

### 1.1 判据换了(原 光速延迟接入计划 11.1)

迷雾的好坏不看它有多真,看它**每分钟给玩家制造几个可以行动的决策**。它唯一的职责是把"信息"变成一种要花代价去买、也可以被对手做假卖给你的东西。按这条判据回头看现有代码,已经有的比缺的多。

### 1.2 玩法骨架已经在数值里(原 光速延迟接入计划 11.2)

按 `sensors/20` 与 `21` 的常数算(是对表里数字做的算术,不是跑出来的实测,但与 `SENS` 自己的注释"静默 `15 万` 稳态 `<1.0` 永点不亮"对得上):

**引擎是一盏灯。** 红外辐射 `E_ir = sigBase + 25×{主推1.0 / 侧推0.6 / 熄火0}`,而 `sigBase` 只有 `0.7`~`1.0`。全速推进约 `26`,熄火滑行约 `1`。通量按 `1/d²`,探测距离因此差 `√26 ≈ 5.1` 倍。

**LADAR 是一把不对称的手电。** 开机射频辐射 `10`,船体泄漏只有 `0.05×sigBase`,被动嗅探距离差 `√200 ≈ 14` 倍;而自己的回波是 `pPing×rcs/d⁴`,四次方,量程只随功率的四次方根涨。代码注释称之为"被嗅≫自照"。而且只有 LADAR 能把接触推到火控级,**想开火就必须开灯,开灯就等于在十四倍远的地方喊"我在这"**。

**因此交战的分段不是舰种属性,是行为决定的:**

| 被看的那一艘 | 状态 | `E_ir` | 点亮(`lit=1`)距离 |
|---|---|---|---|
| CA | 全速推进 | `26` | 约 `60 万 km` |
| CA | 熄火滑行 | `1.0` | 约 `12 万 km` |
| DD | 全速推进 | `25.7` | 约 `53 万 km` |
| DD | 熄火滑行 | `0.7` | 约 `8.7 万 km` |

推导:稳态 `trk = gain/(1−TRK_DECAY) = 10×gain`,要到 `LIT1=1.0` 需 `gain ≥ 0.1`,即 `SNR ≥ 2.45`,于是 `d = √(E_ir/(2.45×FLOOR_IR))`。射频那边同法:开着 LADAR 的 CA 在约 `64 万 km` 外触发 ESM 椭圆(`ESM_ALERT=0.4`),全静默时只有约 `4.5 万 km`。

**更正第 1 节的一处误读**:那张表把 `15 万`/`25 万` 标成"传感器量程",容易读成它门控探测。`detectFor` 里根本不读 `sensorRange`,舰船探测是纯通量对下限比较,**没有距离硬截断**。`sensorRange` 只出现在 `projVisibleTo` 的弹丸可见性、`weapons/52` 的 `macEffRange`,以及绘制圈;`render/83` 第 `375` 行的注释写明三通道改造后它的语义已经是"LADAR 照射距离"。

**一个调数就能修的结构问题**:DD 的 `sensorRange`(`15 万`)正好等于 `macRange`(`15 万`),看见即可打,主炮那一段的迷雾时长是零。迷雾要有"时长",探测距离必须显著大于武器射程。

没有兑现的三个原因:`adminMode` 默认开着,玩家在透视;AI 读真值,不参加这个游戏;UI 完全不演出这几段。

### 1.3 迷雾是一门生意:三种货币(原 光速延迟接入计划 11.3)

玩家只有三样东西可以拿去换信息,全都已经在引擎里:

**辐射**。开 LADAR 换火控级航迹,代价是十四倍远暴露。这是最好的一个,几乎不用写模拟层代码,只需要做成舰队级姿态(静默 / 常规 / 照射),并让玩家**看得见自己此刻有多亮** —— 右栏一个"我方辐射特征"读数比任何教程都管用。

**位置**。前出一艘船当眼睛。光速延迟给了它物理依据(前哨确实带来更早的预警),但就算没有光速延迟,`1/d²` 也已经给了依据。这个角色今天缺的不是机制而是代价与回报的显式化。

**时间**。等一拍再动。`trk` 三通道驻留积分本来就是"时间换确定性",盯得越久等级越高。这条已经完整,只是玩家不知道它在发生;给每条接触一个"正在积累"的进度感就够了。

### 1.4 缺的那一半是欺骗(原 光速延迟接入计划 11.4)

现有的迷雾全是减法:ECM 只让 `trk` 积累打折,诱饵只作用在弹丸层(让导弹扑空或让一部分弹头脱锁),都不进舰船的态势图。ESM 椭圆的猜测中心是 `真值 + 随机方向 × err×0.5`,那是"从真值偏出去",不是"从量测估出来"。

从玩法角度,减法迷雾的问题不是不真实,是**不好玩**:玩家只是看得更少、做得更少,没有新决策。加法才是乐趣所在,它把迷雾从"我看不清"变成"我们俩都在骗对方"。最便宜的三个,按性价比排:

1. **诱饵信标**。信标已经是一种弹丸(`type==='beacon'`,自带 LADAR 平台属性),加一个辐射舰船量级红外特征的变体,敌方态势图上立刻多出一条假接触,成本是一发弹。**这是全场性价比最高的改动** —— 它一次性把"假接触"引进系统,而假接触是关联、欺骗、佯动全部玩法的前提。
2. **熄火潜行**。机制已经有了(滑行 `5.1` 倍隐蔽),只差一个玩家会主动用的动作:接受不能加速的代价换隐蔽。不需要新机制,只需要一个按钮和一个读数。
3. **假航向**。带着引擎光亮走一段,熄火,转向。敌人的外推(`seenBluePos + seenBlueVel × age`)会继续沿旧航向长大。这一条**零代码**,今天没人用只是因为没人知道敌人在外推。

### 1.5 为了好玩要主动放弃的真实(原 光速延迟接入计划 11.5)

- **不做接触的抖动、分裂与合并。** 真实雷达接触会跳、会一分为二,玩起来是灾难。接触位置必须平滑稳定 —— `updateESMFixes` 里 `offDir` 一旦随机定下就不再变(`sensors/21` 第 `50` 行)正是这个直觉,应推广成全局规则:**误差可以大,但不许抖。**
- **不做连续置信度。** 没有 `73%` 这种东西,玩家只能对少数几个能说出名字的档位做决策。现有的 `lit` 0/1/2/3 加"实况 / 陈旧 / 失联"三档年龄已是对的粒度,别再细分。
- **不要求玩家做人工定位解算。** 方位线三角定位是慢节奏潜艇模拟的乐趣,在实时战术游戏里是折磨。系统直接给椭圆,玩家只做"要不要花代价把它缩小"的决定。
- **不对友军加雾。**
- **不做无预兆的惩罚。** 迷雾必须主动告诉玩家他不知道什么。`ladWarned`(被敌方 LADAR 照射时告警)是这个思路的范例且已写好 —— **负面信息做成正面提示**。同理,失联应该是有声音、有日志的事件,而不是像今天这样悄悄衰减掉。
- **不做每帧全量重算。** 态势图按固定节拍刷新(与现有的 `detectLoop` 同拍,每秒一次),玩家读到的是一张稳定、可读、可讨论的战术图。这同时也是第 5 节那条性能约束的答案。

### 1.6 接触要从渲染过滤器变成可操作的对象(原 光速延迟接入计划 11.6)

今天迷雾住在 `drawShip` 里 —— 它决定画不画。这是"迷雾是视觉效果"的典型症状。

如果接触是右栏里一张可以点的列表(编号、年龄、等级、来源通道、一个可缩小的误差椭圆),玩家就可以给它改名、标记"疑似诱饵"、派船去确认、或者对着可能位置圈盲射一轮。迷雾于是从"我看不清"变成"我在办案"。这一条的代价主要在 UI 而不在模拟层,但它决定了整套系统是"让游戏变难"还是"让游戏多一个玩法" —— 是分水岭。

### 1.8 优先级(按"每行代码的乐趣"排序)(原 光速延迟接入计划 11.8)

> **2026-09-18 起这张表被 11.11 取代**(判据换成了"给玩家一个什么决定、什么瞬间")。保留在这儿只为对照当时的排序理由。

| 优先级 | 做什么 | 代价 | 为什么值 |
|---|---|---|---|
| 1 | 接触列表面板 + 点亮/失联做成事件 | UI 为主 | 迷雾从"看不清"变成"能操作",分水岭(11.6) |
| 2 | 我方辐射特征读数 + 静默/常规/照射三档舰队姿态 | 小 | 把已有的手电取舍暴露给玩家(11.3) |
| 3 | 诱饵信标 | 小 | 一次性引进"假接触",后面全部欺骗玩法的前提(11.4) |
| 4 | 探测距离与武器射程拉开 | 调数 | 主炮那一段今天迷雾时长为零(11.2) |
| 5 | AI 改读同一张图 | 大,且要重新标定 | 不做的话前四条只对玩家生效,那不是战争迷雾,是难度调节 |
| 6 | 关联与假接触的完整模型(每方一张接触表) | 大 | 只有在第 3 条已经好玩之后才值得做 |
| 7 | 扩尺度、光速延迟上主菜 | 很大 | 换品类,最后定 |
| 待排 | 对【未认出的接触 / 热区】发射导弹(11.10) | 中 | 用户 2026-09-18 已认可方向;不是当前任务 |

第 5 条的代价要说清楚:AI 一旦读图就会追丢、会打诱饵、会对着幽灵接触开火 —— 这是特性不是缺陷,但会让靶场与全部对局的交战数值一次性漂移,工作量大头在重新平衡而不是改代码。

第 6 条与第 8 节的关系:第 8 节的阶段 A(`LL0`)本来就是接触表的前半步,只是它停在"把八个字段收成一个挂在真船上的 `s.contact` 对象"。既然最终要走到"每方一张表",**A1 要么直接做成表加反查索引,要么就等到第 6 条一起做** —— 做成挂在船上的中间形态会改两遍,而它正是全部改动的中心。同理 `contactPos` 若先落在"读 track"上,光锥层接上去只是给 track 换一个数据源;反过来先接光锥再重构,接线要改两遍。

### 1.9 三条通道现在各自挂什么(2026-09-14 核对)(原 光速延迟接入计划 11.9)

用户问过"是不是一种光学观测挂体型、一种雷达观测挂红外"。核对结果:**游戏里没有光学通道**(`grep 光学|optical` 为空),而且切分不是"光学 / 雷达",是"被动 / 主动"。

| 通道 | 主/被动 | 挂在什么上 | 衰减 | 能给到的最高接触等级 |
|---|---|---|---|---|
| 红外 IR | 被动 | 引擎状态 + `sigBase` | `1/d²` | 单独可给 `lit=1` |
| 射频 ESM | 被动 | 对方开着的 LADAR / ECM + 船体泄漏 | `1/d²` | **单独恒为 `lit=0`**,只能参与交叉 |
| LADAR | 主动 | `rcs` 雷达截面、`pPing` 照射功率 | 回波 `1/d⁴` | `lit=3`(唯一能到火控级的) |

红外不挂在雷达上,它是独立的被动通道;雷达挂 `rcs`,与红外无关。跟"体型"沾边的数据有两个且互相独立:`sigBase`(DD `0.7` / CA `1.0`,同时喂红外底数与射频泄漏)与 `rcs`(DD `0.6` / CA `1.0`,只喂 LADAR 回波)。真正的体型情报在识别层:`shipIdentHull` / `shipIdentTier`(`render/82` 第 `12`~`21` 行)在 `litBlue < 2` 时把轮廓换成 `UNK`、尺寸压成 T2;`sigClassLabel` 在探测级只给"大 / 中 / 小型热源",但它是从 `sigBase` 派生的,不是独立通道。

**用户直觉指向的那个结构已经存在,只是名字不同。** `lit` 阶梯(`sensors/21` 第 `93`~`97` 行)写的是:单条被动通道只知道"有东西";两条通道交叉才到识别级(这就是三角定位);火控级非主动照射不可。射频单独嗅到的辐射源不画成船,画成一个椭圆(`updateESMFixes`)—— 那正是"只有方位、没有身份、不成其为一艘船"的接触形态。这个分工比"光学 vs 雷达"更好,因为它把代价也编进去了:被动免费但模糊,主动精确但暴露。所以第 9 节第 10 条的建议是不加第四条通道。

**核对时发现的两处语义歧义(记下,本轮不改):**

- `FLOOR_IR` / `FLOOR_ESM` 是按**被探测方**的舰种取的(`sensors/21` 第 `83` 行),而这两个常数的注释写着它们是 `=3e-11/detPower`、`=1.2e-11/esmQual` 派生来的,那两个量作用在**探测方**。`ships/11` 第 `40` 行的 ⚠ 注释已经标出这条不变量会静默断掉。直接后果:**一艘船探得远不远,与它自己的 `detPower` 无关**(`detPower` 在舰船探测里根本没被读,只在 `projVisibleTo` 的弹丸可见性和 `formation/39-fmcaps` 的能力维里用)。
- ESM 单独永远给不到 `lit≥1`。这多半是有意的(射频只给方位不给距离),但它意味着一艘**冷而静默但正在辐射**的船在 `lit` 阶梯上完全不存在,只以椭圆形式出现。做接触表时这两种形态要能共存于同一张表。

### 1.10 对未认出的接触、乃至热区发射导弹(2026-09-18 用户认可方向,**不是当前任务**)(原 光速延迟接入计划 11.10)

来由:`态势感知V2.html` 把【等级(航迹质量)】与【身份】拆成了两个正交属性(Link-16 / NTDS 的 Track Quality 与 Identity)。
拆开之前,"没认出就只能 1 级"让等级成了来路的函数 —— 同一个点走进来是 1 级、贴近认出再退回来是 2 级,用户两次报成 bug。
拆开之后的直接推论:一条 2 级、3 级的航迹可以是未识别的,武器门只看椭圆,于是**可以对不知道是什么的东西开火**。用户的判断:可以,记进计划。

分两档,代价差很多:

| 档 | 是什么 | 状态 |
|---|---|---|
| 已定位、未认出的航迹 | 椭圆进了导弹门 / 主炮门,图标是未知 | 演示页里已经是这样(武器门只看等级)。落到引擎时,`lit>=2` 被当成"已识别"用的地方(`shipIdentHull` / `shipIdentTier`、敌舰信息卡)要改读身份字段,武器门照旧读等级 |
| 未定位的热区 | 只有方位、没有位置,地图上是一团热区 | **没做**。它就是 11.6 说的"对着可能位置圈盲射一轮" |

热区盲射要先回答的三件事(都还没定):

1. **瞄哪儿。** 热区的中心刻意不在舰位上(它是从分布里抽的一个样,不是均值),所以"朝热区中心打"天然带着该有的偏差。引擎里现成的入口是区域齐射。
2. **导引头找不找得到。** 弹到了之后按【导引头搜索篮 vs 真实偏差】结算,而不是掷一个命中率 —— 篮子就是 `COV.MSL` 那道门,偏差就是热区半径那个量级,两个数演示页里都有。
3. **值不值。** 这一条才是玩法:那团东西值不值一轮齐射、会不会是诱饵(11.4)。所以它排在诱饵信标(优先级 3)之后才有意思;在那之前做,盲射只是"命中率更低的普通齐射"。

若日后要"必须认出才能打",做成武器侧的一条交战规则开关,**不要塞回等级里** —— 塞回去那个路径依赖会原样回来。

### 1.11 好玩第一,酷是加分(2026-09-18,取代 11.8 的优先级表)(原 光速延迟接入计划 11.11)

用户的原话:做光速延迟、态势感知这些机制是为了**看起来很酷**,整个游戏的目的是**好玩**,理想是这些机制"在好玩的基础上让人眼前一亮"。

**先认一个账。**到这一天为止的十几轮工作几乎全花在把模拟做对上(椭圆滤波、距离梯子、读数与来路无关、与倍速无关)。那是地基,但玩家感觉不到"对",只感觉得到"我做了一个决定,然后发生了一件事"。迷雾只负责藏东西的话,它就只是难度调节。三条通行的判据:

- 它要制造**有代价的选择**(Sid Meier 的"有趣的决定")。
- 不确定性要在下决心**之前**消化掉,不能在之后(输入随机 vs 输出随机):先不知道、再去搞清楚是乐趣;打了却不知道为什么没中是挫败。
- 玩家要**看得懂**自己为什么知道、为什么不知道(可读性)。

**数值里已经藏着一个完整的循环,缺的是动词和瞬间:**摸黑(双方都是热区)→ 谁先亮灯(先拿火控,但在两倍距离上被所有人听见 —— 整套系统里最值钱的博弈)→ 窗口(火控会变质:断照几秒就长出主炮门,对方一干扰就得贴近烧穿)→ 再隐身(静默、滑行,接触衰减成幽灵)。

| 序 | 做什么 | 给玩家什么 | 状态 |
|---|---|---|---|
| 1 | **亮灯有分量**:按下去之前先看清账(亮灯预览),按下去有一下反馈 | 整套系统里最值钱的那个决定,在【做决定的那一刻】把代价与收益摆在地图上 | **演示页已做**(`态势感知V3.html` 场景⑨,见下)。第一版做成了"波前以光速传开",用户 2026-09-18 否掉:**雷达与机动不与光速挂钩** |
| 2 | **暴露度读数**:"我现在多远被看见、多远被听见"做成常驻的舰队读数 | 潜行游戏的光宝石(Thief 的 light gem):随时知道自己有多暴露,隐蔽才是玩法 | **试过,撤掉了**(2026-09-18,见下「第 2 条为什么不成立」) |
| 3 | **机动 = 暴露**:引擎满推让你亮一倍,做成选择 —— 冷滑行(看不见但航线早早定死)vs 点火机动(灵活但全场可见) | 运动内核与感知咬合;系统互相牵制才有深度 | 未做(引擎档的变化即时生效,**不与光速挂钩**) |
| 4 | **接触是可以下令的对象**:派船去确认 / 保持跟踪 / 打(原 11.6) | 迷雾从"我看不清"变成"我在办案"(Cold Waters 的 TMA) | 未做 |
| 5 | **诱饵与盲射**:诱饵/信标造出假热区;对热区盲射(11.10)排在它之后 | 没有欺骗,"身份"这个属性就没有玩法 | 未做 |
| 6 | **AI 读同一张图** | 否则前五条只对玩家生效;AI 被诱饵骗本身就是看自动战斗的乐趣 | 未做,代价最大 |

**为了好玩要主动砍的:**不逐舰管发射档(舰队级三档姿态,单舰可例外);不让玩家看见滤波器(玩家只需要四个词:看见=热区、定位=椭圆、认出=轮廓、锁定=方框;三条经验法则:分开站能定位、开雷达能测距、贴近才认得出);10/20/40 分钟的预警时间里必须有事可做,填不满就压缩;距离梯子图是设计者工具,不进玩家界面。

**对标:**Highfleet(雷达/电子侦察/红外三个圈 + 不明接触,已证明好卖)· Nebulous: Fleet Command(航迹质量与烧穿)· Cold Waters(TMA)· Command: Modern Operations("真实不等于有趣"的反面教材)。一句话的卖点:**一个你能看见自己知道什么的游戏**。

**第 1 条在演示页里的做法:**【亮灯预览】—— 鼠标悬在卡片的「照射 / 干扰」钮上、还没按下去的时候,地图上当场画出这一档会带来的圈:照射 = 紫圈"你照得到多远"(对标准目标)+ 青圈"多远会听见你";干扰 = 青圈(比照射再大 1.41 倍)+ 黄色的烧穿圈(比它近,干扰压不住对方雷达)。已定位的接触若落在圈里,在它身上标"照得到 / 会听见你"。按下去之后放一圈 0.9 秒的脉冲作为反馈(走墙钟,不代表传播时间)。**迷雾规矩:只标【已定位】的接触** —— 热区的距离玩家并不知道,圈画出来,在不在圈里由玩家自己看着判断;替他判了,就等于在按下去之前把热区的距离漏出去。我方视角下也不画对方的脉冲(圆心就是它的位置)。引擎里同形的先例:RF2 悬停武器钮 → 画射程圈。

**被否掉的第一版(记下免得重走):**把发射档的改变做成以光速传开 —— 每艘舰记一份发射档变更记录,凡是问"对方现在什么档"的地方改问"d/c 秒之前它什么档",于是开机后对方 d/c 秒才听见、我 2d/c 秒才有回波。物理上自洽、判据也全过,但用户的判断是:**雷达和机动都不要和光速挂钩** —— 机动时引擎档变来变去,每一次变化都是一个要传播的事件,计算变多,而玩法上没有太大必要。结论:光速延迟留在它自己那条线上(敌我之间的观测时差、命中倒计时),不渗进感知的发射档与引擎档。

**第 2 条为什么不成立(做出来之后用户的原话:"感觉没有什么用,特别是开雷达后,1 看得见你、1 听得见你,不知道是什么意思"):**做的是一块常驻面板 —— 三档宝石 + 被看见/被听见两个半径 + "已定位接触里有几个在我的圈里"。它失败的原因不在文案,在形态:①光宝石在潜行游戏里有用,是因为暴露度随【位置】连续变化、玩家靠走位去改它;这里的暴露度只是两个开关(引擎档、发射档)的函数,而开关是玩家自己刚按的 —— 面板在复述玩家已经知道的事。②"1 看得见你"是个推断(按我的亮度它看得见我),玩家拿它做不了任何决定;只有一个敌人时它还是一句废话。③数字脱离了地图:60 万、240 万公里有没有意义,只取决于"那一圈里有谁",而这件事只有画在地图上、贴在接触身上才读得出来。**教训:信息只在两个地方值钱 —— 贴在它说的那个东西身上,或者出现在做决定的那一刻。常驻的仪表两头都不占。**替代方向(未做,等拍板):【亮灯预览】—— 鼠标悬在「照射」钮上时,地图上当场画出两个圈:内圈"你会看见的范围"、外圈"会听见你的范围",并把落在内圈里的热区点亮("这几团会变成位置")。它回答的是玩家按下去之前真正想问的那句"值不值"。引擎里已有同形的先例(RF2:悬停武器钮 → 画射程圈)。

### 1.12 从 `光速延迟接入计划.md` 待拍板表迁来的两行

| 原行号 | 问题 | 选项 | 建议 |
|---|---|---|---|
| 9 | 接触列表面板是否先做 | 现在就做(不依赖任何模拟层改动)/ 等接触表建好再做 | 现在就做。先让迷雾可读,再让迷雾生效,这个顺序能让后面每一步都立刻看得见效果 |
| 10 | 要不要加第四条"光学"通道 | 加 / 不加,把这个角色并进现有的被动红外 | 不加。见 1.9:现有的"被动只给方位与粗判、主动才给精确位置"分工已经覆盖了这个意图,加第四条只是多一张表 |

## 2. 战争迷雾 / 态势感知:落地待办

### 2.0 落地进度(2026-09-19;用户拍板"做进游戏里面")

分六个阶段。**0~2 已完成并通过全部判定**(`tools/verify.sh` ✓,`tools/sn6_p2.txt`):

| 阶段 | 做了什么 | 状态 |
|---|---|---|
| 0 | 纯增 `js/sensors/23-cov.js`(梯子 + 反解 + 椭圆代数),不接线;与演示页逐位对表 | ✓ 15 常数 + 60 梯子读数逐位相同 |
| 1 | 换内核:`detectFor` 的驻留段 → `stepCov`/`covLit`;靶场重新对齐;删掉驻留那一套 | ✓ |
| 2 | 接触层:定得出位置画误差椭圆,定不出画【热区场】(一坨,不是同心椭圆);补上一个新出现的迷雾泄漏 | ✓ |
| 2.5 | 三级星图(`render/80-viewtier`):层界与缩放上下限由梯子推出、连续交叉淡化、跳层三钮;三层各自的地面语言(公里网格 / 光秒网格 / 距离环+方位辐条);天改成屏幕空间、删掉程序星云 | ✓ |
| 2.6 | LOD 聚合层(`render/82-lod`):舰 → 舰队框 → 舰队组,按屏幕像素带迟滞;红方只聚已定位接触、构成打码 | ✓ |
| 2.7 | 信号视野(右下角工具钮 `#tools` 第一个):把感知模型反过来用,画我方每艘舰的【被探测范围】——【被看见】恒有、【被听见】只在发射时有;半径直读量程律,渐隐填充不画硬边界 | ✓ |
| 2.8 | SN6b:开局三舰成**阵型编队**;靶阵外推到**1 光秒**(摸黑接敌,开局主炮打不响是刻意的);**平滑缩放**(滚轮只写目标 + 对数逼近 + 光标锚点);跳层三钮与信号视野钮一并搬到右下角 `#tools` | ✓ |
| 2.9 | SN6c:开局画面改成**真·迷雾态** —— GM 默认关、靶场蓝方开局**静默**、`initFleet` 末尾补跑一拍感知、三舰**放到**阵型站位上。打开就是三坨热区,没有一个敌舰舰标 | ✓ |
| 2.10 | SN6d:**接触位置唯一化** `contactPos` —— 修掉「鼠标能把只有热区的接触扫出真实坐标」这个泄漏;画在哪/点在哪/聚在哪收成一个出处 | ✓ |
| 2.12 | SN6e:**幽灵/陈旧改画记号**(空心圈+不确定圈+过期时长),不再是压了透明度的舰船图标 —— 顺带堵掉速度箭头/尾焰/命令连线/朝向四样跟着真值实时更新的泄漏 | ✓ |
| 2.13 | SN6f:**接触显示收成一个状态机** `contactState`(none/heat/live/coast/ghost,全部从椭圆派生、五态互斥),四个显示层与聚合层只许问它;`seen*` 改成「定得出位置才刷新」 | ✓ |
| 2.14 | SN7:**定位几何小窗(缩圈图)**做进引擎 —— 右下角「缩圈」钮呼出,悬停 / 左键点敌舰常驻 / 点我方舰看它的攻击目标;多镜头对抗审查确认 11 条问题全部修掉 | ✓ |
| 2.15 | SN7b:修掉「网页卡卡的」—— 星空逐颗画(SN6 带进来的回归)改成两张离屏贴图,90 秒慢帧 47 → 0 | ✓ |
| 2.16 | SN7c:地图上显示敌方**观测等级**(`◎ 3级·火控 ±1.6k x 0.9k`、火控级实线 + 四角火控框),等级配色统一成演示页的 灰/蓝/青/黄,地图 / 标签 / 缩圈小窗读同一张表 | ✓ |
| 2.17 | SN7d:网格改成**固定资产 + LOD**,战区层的放射距离环删掉(与舰队层同一张光秒网格);线改轴对齐矩形 —— 高分屏(DPR=2)上舰队层平移 25fps → 240fps。顺带修掉舰队框「编队[object Object]」 | ✓ |
| 2.18 | SN8:三级星图的**换挡感**(换层大字 + 扫描线、四边刻度尺重新长出来、跳层过冲)+ **聚合动画**(舰船滑进 / 滑出舰队框) | ✓ |
| 2.19 | SN9:**舰体大小随缩放变**(亚线性全程响应:拉近变大、拉远变小,战术落点上与改前同大;全场同一个系数,不泄漏情报) | ✓ |
| 2.20 | SN9b:修**层界冻在加载期的 750px 上**(大屏上按「战区」亮的还是「舰队」、战区那一屏只有一半是战区层的画法;结果还看来路)| ✓ |
| 2.21 | UI2:右下角工具栏**真的**挪到画面右下角(原来在事件窗左边),给底部指令栏实测让位;「缩圈」「信号视野」换成图标钮 | ✓ |
| 3 | 尺度:开局 120 万 + 接触降速(TC1,只在对局里)| ✓ 对局里做了。「战场 200 万」不是一个要改的数:引擎没有战场边界(`CFG.world` 只管星空与开局镜头)。靶场与预设场景的开局间距没动 |
| 4 | 玩家决定开火 | ✓ 半条:查下来蓝方本来就要中键点目标才交战(`autoEngage` 默认关),缺的是**代价** —— FX1 开火暴露已做。**对未定位接触 / 热区开火仍未做** |
| 5 | 敌方 AI 读自己的接触图而不是真值 | ✓ AI1(`61-enemy` + `60-tasks`)|
| 6 | **对局**(MT1):顶栏入口、3 对 3 盲斗、红方随机摆位、结果卡片 | ✓。详见 `js/scenario/CLAUDE.md` |
| 10 | RWR1 被照射告警换成朝照射源方位的弧;HUD1 去掉「已点亮 N 艘敌舰」 | ✓ 2026-09-22 |
| 11 | EM1 开雷达后的表现(涟漪;敌方只在我方听见时画)+ 那个雷达量程大圈拍板 B(不画,悬停「发射档」钮才画两圈) | ✓ 2026-09-22 |
| 9 | MAP1 星图比例尺独立(短边 2 / 15 / 45 光秒,不再拿主炮射程 / 雷达发现 / 被听见当锚)+ SZ1 舰标调小、拉远换记号 | ✓ 2026-09-22。演示页的视图层仍从梯子推,有意分家 |
| 8 | ID2 被动射频不给身份(「热区 → 直接变成舰艇」的根因)+ GM1 地图椭圆跟着「缩圈」钮走、默认不画 | ✓ 2026-09-22 |
| 7 | **形态 H**(H1,2026-09-22 用户拍板):发现域拉长(雷达 160 万 / 被听见 320 万)、对局开局 300 万 = 10 光秒、倍速档位 0.1 ~ 20(RT1)| ✓ 演示页 + 引擎。满推的光学可见距离那个问题,用户的回答是 **RV1:反推单列一档、比主推更亮**(熄火 / 主推 / 反推 = x1 / x2 / x3,DD 24 / 48 / 72 万)✓;主推本身没动,试玩之后再看够不够;光速延迟现在有 10 秒的量级了,接入计划见 `光速延迟接入计划.md` |

⚠ 2.8 只把【靶场】拉到 1 光秒,**阶段 3 仍然没做**:引擎战场还是 100 万、非靶场的对局预设开局仍是 60 万。

⚠ **阶段 3 不做的话,接敌阶段整个不存在**:引擎战场还是 100 万、开局 60 万,而新梯子的雷达发现是 65 万 ——
开局就互相照得到,属于"遭遇"开局(`openClass` 的分类)。热区、亮灯、摸黑那一段在当前尺度下看不到。

踩过的坑与每一处连带改动写在 `js/sensors/CLAUDE.md` 的 SN6 一节。

活页是 `demos/sensors/态势感知V3.html`(误差椭圆模型;2026-09-19 起只有一组数,原「合成」版)。引擎 `js/sensors/` 仍是 SN4 驻留阶梯,两者不能只搬一半(见 `js/sensors/CLAUDE.md`)。

等拍板 / 等试玩的:

- **姿态差取几。**现在光学(满推)/ 光学(冷)= 2 倍(24 / 48 万)。试过 16 / 48、12 / 48:账面与梯子顺序都不变,冷船更难被被动光学看见 ⇒ 更逼着对方亮雷达。⚠ 真空里滑行不掉速,"熄火"的代价是不能机动而不是慢。要试玩才知道。数据在 `态势感知的问题.md` 8.2。
- **战场够不够大(预算 B6.1)。**战场 200 万 < 2.5 x 最远的雷达发现 114 万。2.5 这个倍数是拍的;zcode 的宪章用的是 0.9 x 战场。
- **接触降速的三档(x6 / x4 / x2)手感。**按"每一带 10~120 真实秒"倒推的,没试玩过。联机时倍率共享,"只读我方所知"守不住。
- **照射的三道质量门挪进了悬停预览**之后,舰队层上雷达会不会又"没东西可看"(用户早先的抱怨)。不行就改回常驻,几行的事。
- **光学锁定圈(DD 23k)只算单站。**对方两艘静默舰(基线 10 万)78k 就能对熄火静默的 DD 拿到火控级,是这一圈的 3 倍多。要么按对方实际的观测站数算,要么也挪进"需要时才显示"。
- **BB / CV 是 CA 的克隆**,三个 tier 数值相同。开局 120 万对最远雷达发现 114 万只有 5% 余量,舰种一变大就翻(预算 B6.2)。
- **热区的中心仍然等于真值。**模型里 `covSolve` 只算不确定度、不模拟估计误差,所以 `c.x/c.y` 就是敌舰真实坐标。
  热区画成没有轮廓、没有中心点、不写坐标的弥散场,是为了让人读不出那个中心(老实现是把中心随机偏开一段),
  但一个铁了心的玩家仍然能量出来。真正的解法是**把估计误差放进模型**(让 `c.x/c.y` 按协方差抖一个偏差),
  那会连带改掉"武器打的是估计位置"这件事的手感 —— 属于另一轮。
- ~~舰体按命中体积缩放没搬~~ → SN9 已做,但换了一条律(演示页那条只放大不缩小)。**遗留**:聚合阈值 `LOD.FLEET_PX` / `GROUP_PX` 是按改前的固定图标定的,图标缩小之后舰队可以更晚才塌成框 —— 等试玩手感再定;指数 0.4 与上下限 0.5 / 1.9 同理。
- **靶场的天花板**:CA 照一艘 DD 拿到火控级只有 172,829 公里,所以"三个靶都摆远"和"主炮打得响"是矛盾的。
  现在改成梯度(近靶 12 万进火控级、两个远靶 22.7 万只到跟踪级 = 只能用导弹)。要让远靶也能开炮,
  靶场面板的【体型】与【隐身】两个旋钮就是那道门的分母,不用改场景。
- **右下角工具钮还只做了一个。**用户 2026-09-19:"先做1个,就是信号视野"。这排钮(`#tools`)是个容器,下一个做什么没定。候选:热区开关、误差椭圆开关、距离环开关、EMCON 一键静默。
- 计划里已认可方向、还没排期的:1.10(对未认出的接触、乃至热区发射导弹)、1.11 表里的第 3~6 条(机动 = 暴露、接触可下令、诱饵、AI 同图)。

已知、未修的引擎缺陷:

- **引擎的接触等级有棘轮**(`js/sensors/CLAUDE.md` 有实测):目标被照过一次之后,同一站位上 A 舰读 1 级、C 舰读 2 级。演示页的椭圆模型里没有这个问题(等级只看椭圆)。落地时一起解决,不单修。

落地顺序(建议,未拍板):先搬纯函数层(三条量程律 + 椭圆滤波,`js/sensors/22-percep` 的位置)并用 `tools/verify.sh` 对表 → 再换 `21-detect` 的等级派生 → 最后换渲染(热区 / 椭圆)。数值一律从演示页的 `LAD` 反解出来,不在引擎里另填。

### 2.11 引擎审计待办(2026-09-19,SN6d 那一轮顺带查出来的)

审计口径:光速不变 / 态势感知两条线上的 bug、接缝、逻辑不清、冗余、维护性、未解耦。
**已修的是 A(接触位置唯一化,见 2.10)**;下面是**查实了、这一轮没动**的。每条都带取证方式,别再从头查一遍。

#### B 等级 ⟂ 身份 被违反(下一个该做的)

SN6 的铁律之一是「等级只看椭圆、**身份是另一个属性**」,而 `render/82` 用**等级**推身份:

| 在哪 | 判据 | 遮的是 |
|---|---|---|
| `82-ship-icons` `shipIdentHull` | `litBlue===1` | 轮廓 → UNK |
| `82-ship-icons` `shipIdentTier` | `litBlue<2` | 尺寸 → T2 |
| `87-fleetcards:23` | `litBlue>=2` | 分级徽标 |
| `74-targeting` `xhName`/`masked` | `litBlue<2` | 真名 / 舰种行 |
| `82-lod` `lodComp` | **`covB.idn`** ← 唯一按模型来的 |

实测开局:三个靶 `litBlue=1` 但 **`idn=true`** —— 模型说认出来了,图标说未识别。
而且同屏可以一边显示「群·3 DD×3」、一边三个 UNK 轮廓(两处两个判据)。
**改法**:加 `contactIdn(s,side)` 谓词(与 `contactPos` 并列),前四处全部改读它。
**会看得见**:UNK 从此只在「定得出位置、但没认出身份」时出现;靶场跑十几秒后 靶·B 会从 UNK 变成真实 DD 轮廓。
觉得太容易认出来的话,要调的是梯子上的 `optIdent`/`radarIdent`,**不是把判据换回 `lit`**。
(2026-09-22 ID3 正是这么调的 —— 往**反**方向:嫌太难认出来。见 `js/sensors/CLAUDE.md` 的 ID3 节。)

#### C 三套并行的感知模型(未解耦)

- **C1 椭圆对玩法零影响。** 读点统计:`litBlue`/`litRed` 55+26 处遍布全库;`covB`/`covR` 14+10 处
  **全部在 `render/` 与 `sensors/`,`weapons/` 与 `bots/` 一处都没有**。玩法 100% 由 `lit` 的二值门驱动,
  整套椭圆只用来画画。这是 2.3 的精确版本,要等阶段 4。
- **C2 导弹自带第二套。** `MSL_CFG{passive:100000, ladar:150000}`(`54-missiles`)不走三条量程律;
  `missSee` 里 `d<150000` 那一档**完全不看目标反射/隐身**。`SENS.PROJ`(弹丸可见性)是第三套。
- **C3 `NET_COMM=150000`**(`53-nets`,导弹网内通信距离,超了自毁)与「我方量子实时通信、全舰一体」
  这条设计理念**直接冲突**。属武器系统,要先拍板规范。

#### D 光速延迟:引擎里一行都没有

`C_LS` 在 `js/` 里只出现两处 —— `81-background`(画光秒网格标尺)与 `23-cov`(定义 `AMAX`)。感知是瞬时的。
「你看到的是过去」在演示页有、引擎没有。见 `光速延迟接入计划.md`。

#### E AI 读真值 —— ✓ AI1 已修(2026-09-21),下面是当时的现场记录

- `61-enemy` `enemyAI`:`cx,cy` = **全部蓝舰真实位置的重心**,不看 `litRed` —— 红方从第一帧就知道你在哪并朝你推进;
  无接触时 `pool` 回退到全体蓝舰真值算 `nearest`/`d`。
- `60-tasks`:门是 `litBlue>=2`,之后一律用 `e.pos` 真值下令(`orders=[{pos:nr.pos.slice()}]`)——
  应该改读 `contactPos`(2.10 已经把函数备好了)。

#### J 对局试玩之后要回答的事(MT1 留下的,**需要真人试玩,不是再写一轮机制**)

整局模拟(替身玩家:全队开照射 + 开火控)修完 `driftFire` 那个 bug 之后是 **16 胜 0 负**,每局只损失 0~2 艘。含义:

- **今天的胜负手是主炮对射的站桩优势,不是迷雾。** 红方冲进射程、停车、再转头瞄准;等着它的一方炮口早就摆好了。
  候选:红方进射程前就预先转向(边走边瞄,它自己没有 `driftFire`);或者不停车;或者先用导弹消耗再进炮程。
- **亮灯的代价没有咬人。** 全队一直开照射稳赢 ⇒ 「谁先亮灯」这个决定今天没有分量。候选:红方听见照射就对着方位线盲射导弹(1.10 / 11.10 那条,
  正好也是「对未定位接触开火」还没做的那一半);红方僚舰保持静默绕到侧翼(它已经会横向拉开站位了)。
- 玩十局,每局记:我做了哪几个决定、哪一段在干等。决定少于每分钟一个的那一段就是下一轮要修的地方。
- 红方 AI 的常数(`AIR.LEAD / MEM_S / SPREAD / RING`)、开火暴露(`P_FIRE / FIRE_S`)、降速三档都没经过试玩标定。
- 教程(`85-tutorial` 的 `TUT_HTML`)还没写对局这一节。

#### G 定位几何小窗的遗留(SN7 审查留下的)

- **视线仍是渲染期现查真值**(`sensePairAt`),这一轮只做了止血(量测断了不画 + 整拍冻结、逐目标缓存)。
  彻底的解法是让 `stepCov` 逐站循环顺手记下当拍的探测站与通道(`c.src`),小窗只读记录 —— 那要动与演示页逐位对表的内核。
- **主地图上既有的两处真值连线**(审查核查员顺带指出,非 SN7 引入):`83-hud` 的 `drawLocks` 直接连到 `lockedTarget.pos`、
  `drawFcChain` 直接连到序列目标的 `t.pos`。目标退回热区 / 失联之后这两条线仍指着真值。应改读 `contactPos`,交代不出位置就不画。

#### I 语义缩放的内容那一半(SN8 的 B 方向,等拍板)

SN8 只做了「换挡的手感」(A)和聚合动画(C)。真正让三层不只是换颜色的是**每层看不同的东西**,而每层放什么是玩法决定:

| 层 | 主角 | 候选内容 |
|---|---|---|
| 战术 | 单舰 | 武器射程、速度矢量、阵型站位(现在都有)|
| 舰队 | 舰队 | 每支舰队一条**合成包线**(武器够得着 / 雷达覆盖 / 被听见),按舰队只画一条外轮廓,不再每船一圈 |
| 战区 | 航迹 | 舰队与接触的**航迹尾巴**;热区加粗;**信息时延读数**(「这条接触是 3.2 秒前的样子」),为光速延迟接入留位置 |

⚠ 性能红线(SN7d):不画整屏大 path、不每帧画成批大圆。合成包线要么缓存成贴图,要么控制在每支舰队一两个圆。

#### H 雷达覆盖圈在战术层上看不见(2026-09-21 实测,未动)

选中我方舰时 `84-scene` 一直在画它的照射圈,但 SN6 换梯子之后照射发现距离从二十几万涨到 **114 万公里**(CA 对标准目标),
开局缩放下屏幕半径 2295px,比整个画面还大;线又只有 15% 不透明度。底栏「发射档」钮刻意没有 hover 圈(`88-selpanel` 里写着:照射圈依赖目标反射)。
武器那三种圈(主炮 / 导弹 / 拦截)悬停底栏钮就出,**用户 2026-09-21 确认这样没问题**,右下角加「射程圈」常驻钮的提议作废。
雷达圈要不要补一个看得见的表达(比如圈在画外时在画面边上留一行标注,信号视野已经是这么处理的),等拍板。

#### F 冗余 / 维护性

- **F1 `mslRange` 的兜底纪律。** `11-classes:116` 烘焙时就写了 `mslRange:lw.mslRange||350000`,
  所以 `83-hud:621` 那句「显式化 `R=0` 免得给出打得着的假象」的修法**今天并不生效**。
  隔壁 `macRange`/`macRadar` 用的是 `sReq`(缺失就抛)+ `resolveLoadout` 显式烘 0。
  **改法**:照 `resolveLoadout:44` 那条 mac 先例加一条对称的 `if(!('missDmg'in src)){...=0}`,再把烘焙改成 `sReq`。
  **今天不可达**(四个舰种全带导弹,CV 也有 `msl_heavy`)—— 是纪律修补,不是现在看得见的 bug。
- ~~F2~~(**已随 SN6f 解决**:旧口径的 `stale` 整个没了)**`contactState` 对纯方位接触返回 `stale`**(`seenBlue` 只在有位置时写)——「正在听着」被叫成「陈旧」。
  2.10 之后后果被彻底兜死(`contactPos` 对没有 `seenBluePos` 的一律返回 null),语义仍别扭。
- **F3 `contactPos` 的 Z 轴取 `s.pos[2]`(真值)。** 椭圆模型是二维的,高度不在模型里 —— 原样保留的既有行为。
- ~~F5 实况红方接触的【命令连线】仍然画着~~ → **FG1 已修(2026-09-21)**,连同来袭走廊的来源线起点,见 `js/scenario/CLAUDE.md`。原记录:(`82-ship-icons` 尾部读 `s.orders[0].pos`)。位置我确实知道,
  但它接下来要去哪永远不该知道 —— 与 SN6e 堵掉的那四样同类,只是那一档不在用户当时的说法里,没动。

### 2.1 姿态对抗表:现在"全程照射"是占优策略(2026-09-19 实测)

问题是"亮不亮灯"这个核心决定到底有没有意思。按 Sid Meier 的定义,有意思的决定不能有占优选项 —— 这是可以量的。
做法:在 `demos/sensors/态势感知V3.html` 上挂探针,2v2 DD、各自两舰横向间距 10 万、对头 1000 km/s 接近,两边各用一种固定姿态,
记每一方第一次对对方拿到跟踪级(导弹可用,且须在 35 万之内)的距离,相减 = **先手量**(万公里;正 = 行那一方先开火)。

| 我 \ 对手 | 全程照射 | 静默·熄火 | 静默·满推 | 听见/看见就开 | 全程干扰 |
|---|---|---|---|---|---|
| 全程照射 | 0 | +11 | +11 | 0 | +5 |
| 静默·熄火 | -11 | 0 | +8 | 0 | +6 |
| 静默·满推 | -11 | -8 | 0 | -11 | -2 |
| 听见/看见就开 | 0 | 0 | +11 | 0 | +5 |
| 全程干扰 | -5 | -6 | +2 | -5 | 0 |

(主炮先手量同形:全程照射那一行是 0 / +9 / +9 / 0 / +5。)

两条结论:

1. **"全程照射"那一行没有一个负数 —— 它是占优策略。**被听见(124 万)没有任何后果:听的一方只有方位,两站交会要到 35 万才定得出位置、24 万才到跟踪级,而照射的一方 41 万就有跟踪级。"亮灯的代价"在模型里存在,在博弈里不存在,因为没有任何东西能对一个只有方位的辐射源下手。
2. **在占优姿态下,椭圆那一整套不起约束。**两边都照射时 58 万就是跟踪级、17 万就是火控级,都在武器射程(35 / 15 万)之外 —— 进射程之前解早就有了,"争夺火控解"这场赛跑根本没发生。椭圆只在被占优的那几种姿态(被动)里才是瓶颈。干扰能把照射方的跟踪级压到 29 万、火控压到 11 万(压进射程之内,赛跑出现了),但干扰方自己是聋的,仍然输 5 万。

所以现在的状态是:模型精确、读得懂,但**核心决定没有牙**。要让它有牙,候选(都没做、没验证,改完要重跑这张表看占优是否消失):

- **对辐射源开火**(反辐射 / home-on-emit;就是 1.10 认可过的那个方向)。它不是锦上添花,是"亮灯有代价"成立的前提。
- **让照射对【静默目标】的跟踪级距离落进导弹射程之内**(现在 41~58 万 > 35 万),这样开着雷达也得赛跑,静默 / 干扰才有东西可争。
- 表里只有纯策略、对称的 2v2。真正可能有戏的是**混合分工**(后方一艘照射当"灯"、前出的静默舰借共享态势图开火 —— 我方全舰一体的设定正好支持)与几何(基线、前出哨戒),这些还没量。
- 探针的局限:先手量只是代理指标(没算导弹飞行与拦截、没算"被听见之后对方可以绕开 / 设伏"),没有 AI,没有胜负。

**现在不能玩**:演示页是模型观察器,没有对手、没有赌注、没有后果;引擎里有武器和胜负,但敌方 AI 不在同一张迷雾下(1.11 第 6 条),感知还是 SN4。要回答"好不好玩",下一步不是再打磨演示页,而是:先用这张表把规则改到没有占优策略 → 再在引擎里做一个最小的对局切片(2v2、同迷雾 AI、对辐射源开火、有胜负)。

### 2.2 基线对抗表:"散开还是聚拢"是现在**唯一**有牙的决定(2026-09-19 实测)

同一套探针,换一个问题:两艘静默蓝舰,横向基线 B,去定位一艘红舰。量第一次定得出位置 / 第一次到跟踪级的距离。

| 基线 | 红舰全程照射 | 红舰静默·满推 | 红舰静默·熄火 |
|---|---|---|---|
| 2 万 | 24 万 / 16 万 | 26 万 / 18 万 | 20 万 / 14 万 |
| 5 万 | 28 万 / 22 万 | 35 万 / 24 万 | 23 万 / 19 万 |
| 10 万 | 36 万 / 25 万 | 44 万 / 31 万 | 23 万 / 23 万 |
| 20 万 | 44 万 / 29 万 | 46 万 / 37 万 | 21 万 / 21 万 |
| 40 万 | 54 万 / 35 万 | 42 万 / 42 万 | 13 万 / 13 万 |
| 80 万 | 61 万 / 跟不上 | 26 万 / 26 万 | **一直没发现** |

(格式:定得出位置 / 跟踪级。对照:编队舰间距下限 2 万、屏护半径 5 万、近防外圈 2.5 万。)

**这张表里有一个真的决定,而且它现在就有牙:**

- 对着**吵的**目标,散开线性地赚:基线 2 万 → 80 万,定位距离 24 万 → 61 万,**2.5 倍**。
- 对着**安静的**目标,散开先平后亏:光学冷发现只有 24 万,基线一旦超过它,两艘船就没有一艘离目标够近;80 万基线下**整局都没发现它**。
- 而散开还要付第三份钱:屏护半径 5 万、近防外圈 2.5 万 —— 散到 20 万以上,两艘船的近防互相够不着,导弹来了各打各的。

所以"散开多少"同时被三件事拉扯:**对辐射源的定位速度 / 对静默船的发现能力 / 防空的互相掩护**,而且最优解取决于**对手在干什么**(他吵我就散,他静我就收)。这正是"有意思的决定"的定义 —— 没有占优解,而且要读对手。

⚠ 但它被 2.1 那个问题压着:既然亮灯没有代价,对手就总是吵的,于是"散开"永远对,张力又塌回去了。**先修 2.1,这条才活。**

### 2.3 更深的一层:开火是自动的,所以椭圆对玩家没有后果

比 2.1 更根本的一条。现在火控门是**二值**的:椭圆进了导弹门 ⇒ 导弹可用;没进 ⇒ 打不了。而且开火由 `autoEngage` 自动做。

后果:**玩家永远不会为"信息够不够"做一次决定** —— 够不够是引擎算的,打不打也是引擎决定的。椭圆做得再细(各向异性、Fisher 信息、三通道融合),在玩家那儿只剩一个布尔量。一整套信息质量系统没有出口。

要让它有出口,得让"信息质量"连续地影响**后果**而不是二值地影响**许可**:
- 对未认出的接触、乃至对热区开火(1.10 已认可方向):椭圆越糊,导引头越可能搜空 ⇒ 代价是**弹药**而不是"不许打"。于是"现在打还是再等等"成为玩家的决定,而且椭圆大小直接标价。
- 这条与 2.1 是一对:2.1 让"亮灯"有代价,2.3 让"信息质量"有用处。只做一条都不够。

## 3. 武器系统:机动-武器(2026-09-19 用户拍板:这部分属于武器系统,不属于战争迷雾)

**这一节只有问题和事实,没有方案。**事实来自 `demos/sensors/态势感知V3.html` 预算面板的「无量纲比」(P1 / P2 / P3 / P9 四行在那一页只摆数、不设目标、不判 —— 判它们是这一节的事)。

引擎现值(出处):主炮射程 15 万、导弹 35 万、主炮装填 30 秒、导弹单元装填 60 秒(`weapons/51-defs`);弹速 3 万 km/s = 0.1c(`core/00-config` `macSpd`);命中判定半径 2000 公里、不分舰种(`weapons/56-step-projectiles:56`);推进加速度 DD 20 / CA 15 km/s²、最高速度档 800 / 700(`ships/11-classes` `CLS_MOB`);结构 DD 550 / CA 900、主炮伤害 DD 220 / CA 400(`ships/11`、`weapons/51-defs`)。

| 比值 | 式子 | 现值 | 它在说什么 |
|---|---|---|---|
| 主炮带齐射数(P1) | 主炮射程 /(对头 1600 x 装填 30) | 3.13 | 对头冲一趟主炮带,主炮打得出三轮。**与时间倍率无关** |
| 击毁所需命中数 | 结构 / 主炮伤害 | 2~5 发(CA 打 DD 2、DD 打 DD 3、CA 打 CA 3、DD 打 CA 5) | |
| 齐射数 / 击毁所需 | 上两行相除 | 0.6~1.5 | **一趟对冲大致决一次生死**;先拿到火控先开一炮 = 先打掉对方 20%~50% 的血(first look, first shot, first kill) |
| 导弹带齐射数(P2) | (导弹 - 主炮)/(1600 x 60) | 2.08 | 同一个单元从进导弹射程到进主炮射程打得出两波 |
| 弹道规避比(P3) | ½·a·(主炮射程 / 弹速)² / 命中半径 | 0.13 | 炮弹飞 5 秒,目标靠【加速】只能偏出 250 公里(匀速那 4000 公里可以预测,不算)。**射程之内机动躲不开炮弹**,命中只由火控质量与引擎的人工散布(`MAC_SPREAD_K`)决定 |
| 刹停距离 / 主炮射程(P9) | v² / 2a / G | 0.11 | 进了主炮带还退得出来 |
| 火控门 / 命中半径(P4,跨感知) | CA 主炮门 / 2000 | 1.00 | 零余量。命中半径若按舰种缩小,感知侧的火控门要跟着动 |

### 3.1 反辐射武器(home-on-emit / ARM)—— 2026-09-19 用户:属于武器系统,写入待办,**先不做**

**它要解决的问题在感知那边**(见 2.1):现在"全程照射"是占优策略,因为亮灯的代价(124 万被听见、被跟踪距离从 35 万涨到 58 万)全部落在武器射程 35 万之外 —— **没有任何东西能对一个只有方位的辐射源下手**,所以那份代价等于没收。真实海军之所以 EMCON,正是因为存在这类武器。

要点(设计时再定,现在只记):

- **只要方位就能发射**,不需要定位、不需要火控级 —— 这是它与现有全部武器的根本区别,也正是它能罚到辐射源的原因。现有的发射门控(`fireMissiles` / `orderMissileSalvo` 的 `q<2`、`fireMAC` 的 `q<3`)对它不适用,要单开一条。
- 目标不是一艘船,是**一个辐射源**。对方一关机(`emitMode` 回 `silent`),弹上就没有目标了 —— 于是"关机"成为一种防御动作,"什么时候关"又是一个决定。
- 射程要够到"被听见"那一档(参考对 125 万),否则代价仍然落在窗口外。这个射程远大于现有导弹 35 万,是**武器系统的尺度问题**(它会进 `todo-plan` 第 3 节那张无量纲比表,也会动尺度预算的 B2 / B8)。
- 与干扰的关系:干扰机比雷达更吵(演示页里干扰的被听见是 140 万 > 照射 124 万),所以 ARM 天然也是反干扰武器。
- 落地顺序:**先有感知系统进引擎、玩家能决定开火,再做它**。它要罚的那个占优策略,只有在玩家与 AI 都在同一张迷雾下才有意义。

验证方式已经现成:改完重跑 2.1 那张姿态对抗表(探针 `duel_posture_matrix_probe.js`),看"全程照射"那一行是否出现负数。

要拍板的:

1. **"机动规避"要不要成为一个决定。**现在不是(P3 = 0.13):防御只有一条路 —— 不让对方拿到火控级(隐身、干扰、先打掉)。这让感知优势极值钱,游戏很锐利,但非黑即白。要让它成为决定,P3 得到 0.5 以上,也就是下面四样里动一样:加速度 x4 / 弹速 ÷2 / 命中半径 ÷4(或按舰种)/ 主炮射程 x2。每一样都牵动别的系统(运动内核调过十几轮;弹速 0.1c 是设定;命中半径牵着火控门 P4 与编队间距;主炮射程是整张尺度预算的量纲基准)。
2. **一趟对冲决生死(0.6~1.5)是不是想要的锐度。**想钝一点就加结构或减伤害,想更锐就反过来;它与第 1 条是同一个旋钮的两面。
3. **命中半径要不要按舰种。**现在 DD 与 CA 同是 2000 公里,而感知侧的主炮门已经按目标尺寸分了(DD 1.4k / CA 2.0k)。
4. 光速延迟那边的"命中倒计时 / 弹道提前量"(`光速延迟接入计划.md` 第 11 节、阶段 B)落地时,用的就是这一节的弹道时间(弹道时间是光延迟的 10 倍)。

## 4. 尺度落引擎

演示页的尺度预算(`BUD` / `budgetRows`)现在是 22 条成立、0 超支、2 条待定。落引擎时要改的引擎数:

- `CFG.world`:半幅 50 万 → 100 万(战场全宽 200 万,用户已拍板的"战役感"档)。
- 六条对局预设的开局间距:约 60 万 → 120 万。60 万在新梯子下是「遭遇」开局(合法,但要当成那一种来摆,不能当标准开局)。
- 靶场的靶(预算 B12,待定):最近的靶斜距 20.2 万,CA 照 DD 的火控距离是 17 万,主炮会哑火 ⇒ 靶往里挪。它取代 `js/sensors/CLAUDE.md` SN5 里"CA 的 emit x recv >= 7.87"那条下限。
- 时间倍率:缺省 x12 + 接触降速(已定位 x6 / 导弹带 x4 / 主炮带 x2)。引擎 `frame()` 里的 `rate` 要变成"滑块上限 + 我方所知的档位"。
- zcode 的宪章提议建 `js/core/00-scale.js` 做尺度的单一真相;没开始,等第 2 节一起。

## 5. 与以上都无关的代码清理(2026-09-19 从 `光速延迟接入计划.md` 7.6 / A4 / 待拍板第 3 行迁来)

| 代码 | 现状 | 建议 |
|---|---|---|
| `outerIntercept` | `weapons/51-defs` 第 13–14 行声明,经 `weapons/51-ciws` 第 9 行、`ships/11` 第 36、45、51、112 行、`scenario/92-editor` 第 18 行一路传递,玩法上零读取(`scenario/95` 第 58 行与 `weapons/52` 第 74 行的注释都写明它是死字段) | 要不要顺手删由用户定(见第 9 节) |
| `radArcText` / `_radNameCache` | `render/89` 第 144 行与第 34 行,自 RF5 起没有调用点(第 237 行注释) | 同上 |
| `FIRE_ALL_ON` 与全弹发射分支 | `command/71` 第 29、269 行,RF6 定案只藏不删 | 不动 |

待拍板(原第 3 行):删 `outerIntercept` 与 `radArcText` / 不删 —— 由用户定。

## 6. 演示页与文档整理

2026-09-19 已做:仓库根的设计演示页归档进 `demos/`,按系统分子目录(与 `js/` 的系统目录同名):

| 目录 | 页 |
|---|---|
| `demos/sensors/` | `态势感知V3.html`(活的)· `态势感知V2.html`、`态势感知.html`(冻结) |
| `demos/lightlag/` | `光速延迟.html` |
| `demos/formation/` | `阵型控制台.html` · `编队三模式.html` · `舰船能力权重.html` |
| `demos/ships/` | `icon-preview-zcode.html` |

没动的:`icons_preview.html` 留在仓库根 —— 它是已提交、已随 GitHub Pages 发布的项目文件(挪了会改线上地址),不是设计演示页。

还没做的:几份 `.md`(本文件、`光速延迟接入计划.md`、`态势感知的问题.md`、`尺度预算.md`)还在仓库根;要不要也收进一个 `docs/`,等拍板。

## 7. 全库代码审查(2026-09-21,只读;62 个文件 / 12,668 行 / 932 个顶层符号)

方法:去注释后做静态分析(符号声明与引用、跨目录耦合矩阵、死符号、`typeof` 守卫的指向、重复写法、每帧路径),可疑点逐个读代码并实测。
脚本没进仓库(一次性的);数字可复现的口径写在各条里。**只有证据的才记,猜的不记。**

| # | 维度 | 发现 | 证据 | 建议 | 状态 |
|---|---|---|---|---|---|
| R1 | 身份 | **「认出」这一级在引擎里是死的**:显示层拿【等级】当【身份】用(`shipIdentHull` 的 `litBlue===1`、`shipIdentTier` / 舰队卡 / 信息卡的 `litBlue<2`、舰标名字的 `identQ===1`),而内核另有 `cov.idn`,只有 `82-lod` 与 `83-geom` 两处在读 | 梯子:CA 照 DD 跟踪级(lit2)门 435k,雷达认出 151k、光学认出 94k。实测 CA 照 30 万外的 DD:`litBlue=2`、`cov.idn=false` ⇒ 地图上画的是 DD 轮廓 + 真名,同一艘船在聚合框里却记成「?」 | 一个访问器 `contactIdn(s,side)`(同 `contactPos` / `contactState` 的做法),上面那几处全部改问它。这就是 2.11 B | ✓ ID1,`FLOW77_IDN` |
| R2 | 逻辑 | **跳层钮「以选中舰为中心」从来没生效过**:`80-viewtier` 的 `camJump` 写的是 `typeof byId==='function' ? byId(...)`,而 `byId` 全库没有声明 ⇒ 守卫恒假、静默走重心 | 全库 429 处 `typeof` 守卫 / 183 个不同符号,指向不存在符号的只有这 1 个 | 改成 `ships.find`;并给 `verify.sh` 加一条机械检查:**守卫指向的符号必须在全库有声明**(符号扫描今天只查「声明了的都在」,不查「被守卫的存在」)| ✓ `FLOW78_JUMPSEL` + 判定块的守卫检查(带自检)|
| R3 | 解耦 | **模拟层依赖呈现层的 `log`**:`sensors / physics / formation / weapons / bots` 五个模拟目录都直接调 `render/86-log` 的 `log()`(35 个文件),它每条做 5 次 DOM 操作,写的还是被 RF2 藏掉的 `#log` | 逆层引用表;`log()` 实测 0.06 ms/条 | 标准形态是观察者 / 事件总线:模拟只 `emit`,界面订阅。最小做法是把汇聚点挪进 `core`(纯移动),`86-log` 与 `88` 的 `pushEvt` 各自订阅 | ✓ `core/02-events`,`FLOW79_LOGBUS` + 判定块的分层检查(带自检);SL1(2026-09-22)起总线连同全部 `log(...)` 调用整体删除,分层检查保留,另加一条「js/ 里不许再有裸 `log(`」的负对照 |
| R4 | 解耦 | `bots/61` 与 `core/06` 读 `scenario/97` 的 `matchIsOn()`(MT1 / TC1 那轮我自己引进来的):模拟读了一个界面模块 | 逆层引用表 | 改读场景数据 `curEnv().match`,或在 `core/01` 放一个对局态 | ✓ 改读 `curEnv().match` |
| R5 | 冗余 | 死符号 4 个:`taskCancel`、`CORNER_K`(根 CLAUDE.md 的状态归属表还写着它是「唯一定义点」)、`radArcText`、`SPEED_NAMES`;另有 6 个只被判据调用(`fmMembers` `fmFollowShip` `fmFollowStop` `senseScanTarget` `senseBoundsAt` `ladCheck`)| 全库只出现声明那一次 | 前 4 个删;后 6 个留(它们是判据的探针)| ✓ 已删(连 `_radNameCache`),这一轮过后顶层符号 932 → 942(删 5 个,新增访问器 / 订阅表 / 七个 md* 共 15 个)|
| R6 | 冗余 | 约 **1,030 行(8%)在伺候被 RF2 藏掉的界面**:`72-context-menu` `73-quickbar` `60-tasks` `92-editor` `93-replay` `94-demo` `86-log` `87-fleetcards`。`updQbarHighlight` 每帧给 5 个看不见的钮写字 | 实测每帧 0.003 ms、`updateCardsStatus` 0.075 ms / 20 帧 —— **不是性能问题,是维护面**:改 HTML id、改舰字段都得照顾它们 | 「只藏不删」是 RF2 的拍板。垂直切片定型之后值得重新问一次:哪些永远不回来 | ✓ SL1(2026-09-22 用户拍板):8 个文件整删(上面那 7 个 + `core/02-events`),`87-fleetcards` 缩到 3 个函数,连带事件系统 / 互搏 / 设置面板 / 顶栏隐藏钮 / css 隐藏节;js 12,746 → 11,255 行,顶层符号 947 → 801,判据 104 → 103(`FLOW79_LOGBUS` 随总线删)。记录在根 CLAUDE.md「RF2 简化 UI 备忘」与 command / render / scenario 三份备忘的 SL1 节 |
| R7 | 冗余 | 重复写法:墙钟取时那个三元 11 处(已有 `_vtNow` / `_zNow` 两个 helper,还是各写各的);`side==='blue'?litBlue:litRed` 6 处;`||350000` / `||150000` 兜底字面量 18 处(烘焙字段缺失时静默给满射程,2.11 F1);`ships.find(x=>x.id===…)` 27 处(没有 id → 舰的索引)| 计数 | 顺手收,不单独排期 | ✓ `nowMs` / `shipById` / `litOf`;下游 16 处兜底字面量删掉(`makeShip` 已烘焙,下游兜底永远触发不了,方向还是 fail-open)|
| R8 | 可维护 | 最大的函数:`onMouseDown` 310 行 / 1 万字符(输入状态机,全库最难改的一块)、`stepMissileProj` 253 行、`updateSelPanel` 156 行、`drawShip` 182 行 | 去注释后的字符数排序 | `onMouseDown` 按键位做一次 RF1 式的纯提取(左 / 中 / 右三个函数)| ✓ 七个 `md*`,逐行对账零差异 |
| R9 | 可维护 | `tools/verify.sh` 6,397 行 / 101 条判据,单文件,已经是引擎的一半大 | 行数 | 判据按系统目录拆成 `tools/judge/*.js`,`verify.sh` 只管拼接与判定 | ✓ 6,537 → 424 行,11 个文件;拼出来的探针页与原来**逐字节相同**;多了一道 `node --check` |
| R10 | 性能 | **没有问题。** 6 舰交战:`stepSim` 0.049 ms/步(x50 每帧 2.1 ms)、`detectLoop` 0.04 ms/拍;40 舰 13 ms / 帧 @x50;80 舰 22 ms(x50 会掉帧,x20 以下无碍)。O(N²) 的感知在 80 舰下才 2 ms / 模拟秒 | 无界面实测;渲染的真 GPU 数字见 `js/render/CLAUDE.md` SN7b / SN7d | 不动 | — |

**健康的部分**(同一次审查量出来的,免得以后白查):`sensors` 只依赖 `core`(外加 `log`);`physics` / `formation` 同样干净;全库**零**处 `cls==='XX'` 硬编码舰种;
没有同名的顶层声明;429 处 `typeof` 守卫里只有 1 处指空;注释与空白占 40%(这是本项目的约定,不是问题)。

## 8. 「停着等他们来、挨个点名」这条占优策略(2026-09-22 用户实报,五项)

用户:「**我只需要开着雷达,跑路到差不多对方航线的位置,然后停着等着他们过来然后我挨个点名就能赢** —— 就在那里等热区变成热信号、再等着进入武器射程,没意思。怎么才能更有意思一点?Sea Power 是怎么做的?」

Sea Power / Cold Waters 那一类的答案是:**接触前的每一秒都要花钱**。具体是四件事 ——
① 传感器与武器之间隔着一大段(我们有了:形态 H);② 开雷达要付账(我们有了:被听见 320 万 + FX1 开火暴露);
③ **AI 不是直线冲过来的**(我们没有);④ **等待本身有成本**:燃料 / 弹药 / 战区外的目标在跑 / 上级在催(我们完全没有)。

拆成五项,按"每行代码的乐趣"排:

| # | 做什么 | 状态 |
|---|---|---|
| 1 | **认出这一级重定** —— 认出的距离在武器带之外,别打完了还是「大型热源」 | ✓ **ID3 已做**(2026-09-22),见 `js/sensors/CLAUDE.md` |
| 2 | **等待有成本** —— 停着不动也在输(时限 / 弹药 / 别处的目标)。这是四件事里我们唯一整块没做的 | 待拍板:做成什么形式(任务时限?护送目标?)要先问用户 |
| 3 | **接触可下令** —— 对着椭圆下令,而不是对着船;对未认出的接触开火(1.10 已认可方向) | 待排,见 1.10 |
| 4 | **姿态是舰队级的决定** —— 谁开雷达、谁静默、谁当诱饵,现在是每舰一个钮 | 待排,见 2.1(「全程照射」是占优策略那一节) |
| 5 | **红方 bot 条令重做** —— 不许直线冲进主炮带;在导弹带边缘打、静默僚舰侧翼、集火、挨打就撤 | ✓ **BOT1 已做**(2026-09-22):指挥层 + 执行层、六态条令、集火 WTA、一盏灯轮换、埋伏有时限、挨打就撤。见 `js/bots/CLAUDE.md`。**没做**:影响图、导弹规避、蓝方侧的战斗机动 |

⚠ 与第 5 项连着的两条实测(`js/weapons/CLAUDE.md` 的 WR1 节):
**「谁停下来谁开得出炮」**(WR1 之后主炮要机头对准,编队里一直在走的一方几乎开不出炮 —— 这正是用户那句"停着等"的力学根源);
以及蓝红导弹命中的不对称(近防 / 齐射节奏 / 编队密度都可能)。
