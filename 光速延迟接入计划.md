# 光速延迟接入计划(底层战争迷雾 · 物理模拟)

> **范围(2026-09-19 整理):这份文件只放光速延迟。**整套战争迷雾的玩法框架、武器系统的设计问题、尺度落引擎的待办、演示页整理、无关的死代码,都在仓库根 `todo-plan.md`。

> 写于 2026-09-11。状态:计划已写出,等待拍板;游戏代码一行未改。本文件与演示页 `光速延迟.html` 都还没有提交。(2026-09-19:演示页搬到了 `demos/lightlag/光速延迟.html`。)

## 0. 这份文件是什么、怎么接着用

这份文件把光速延迟从设计推导、演示页、游戏代码盘点到分阶段接入计划的全部结论放在一处,目的是会话压缩或换会话之后能直接接着干。接着做之前,先读第 2 节(已定案的设定)和第 9 节(还没拍板的问题)。

| 来源 | 位置 | 内容 |
|---|---|---|
| 本文件 | 仓库根 `光速延迟接入计划.md` | 定案、模型、盘点、计划、待拍板 |
| 演示页 | `demos/lightlag/光速延迟.html` | 全部物理实现,自带 `?selftest` 物理自检与 `?uitest` 交互自检 |
| 项目记忆 | `~/.claude/projects/C--Users-21472-Desktop-game/memory/light-lag-design.md` | 设计方向与用户定案的短版,指向本文件 |
| 原始会话 | `~/.claude/projects/C--Users-21472-Desktop-game/ab4c4aac-520f-4814-9a27-0fc025e62513.jsonl` | 逐轮对话:每一版的推导、读数、用户原话 |
| 项目约定 | `CLAUDE.md` | 感知层、`stepSim` 段序、探针与发版规矩 |

原始会话是 JSONL,每行一条记录,`type` 为 `user` 或 `assistant`,正文在 `message.content`。按关键词(例如"计划如下""L0 真值""性能")过滤 assistant 的 text 块,就能找回某一轮的原文。

行号核对于 2026-09-11,对应提交 `806b68d`(FM7b)。写完时仓库已前进到 `6a825a3`(FM9),其间计划引用的 `js/` 文件一个都没动,`index.html` 只改了 `?v=` 版本号,所以这些行号在 `6a825a3` 上仍然成立。另一个会话一直在同时改仓库(核对期间 `index.html` 的 `?v=` 从 `202609070` 升到了 `202609072`),行号以后还会漂,函数名才是稳定的锚点;动手前再 grep 一次。

本文件取代会话里那一版"计划如下"的回复,并修正了其中几处:敌方开火日志的实际行为(见 7.3);A2 的范围,射程判定不跟着搬(见 8.1);新名字避开了已被占用的 `seen` 与 `ct`(见 8.3);阶段 A 的文件清单补上 `command/70`;改动面计数换成可复现的 grep 模式(见 7.5)。

(2026-09-19:第 11 节里与光速延迟无关的部分已迁到 `todo-plan.md`,这里只剩光速延迟自己的定位。以下是当时的说明。)2026-09-14 追加第 11 节:从玩法角度(而不是物理仿真角度)重想的战争迷雾,含优先级清单与"主菜选哪一条路"的分岔。第 1 到 10 节是光速延迟这一条轴的接入方案,第 11 节是整套迷雾的玩法框架,两者的关系是"底座"与"房子" —— 先读 11.8 的优先级表,它决定第 8 节那两个阶段在整体里排第几。第 11 节还更正了第 1 节距离表的一处误读:`sensorRange` 不门控舰船探测(见 11.2)。

## 1. 背景与这一轮的目标

用户把光速延迟定为理想中的核心设计:敌方舰队在几光秒到几光分之外,我方看到的一切都是过去的样子。在系统里它归类为底层战争迷雾系统,属于物理模拟,位于感知层之下。

这一轮用户的要求是先做接入前清理:把系统放进游戏但不急着上线,同时清掉没用的字段和会妨碍接入的代码。不急着上线的理由是,现在的测试要么在光秒以内,要么直接透视,中间还隔着几层玩法上的鸿沟。落到工程上是三件事:

1. 放进游戏,开关默认关闭;关闭时游戏行为与现在逐位一致。
2. 清掉感知链路上没用的字段和重复状态。
3. 把绕过感知、直接读敌方真实状态的写法收口到少数几个取数函数,给以后接入留好插口。

这一轮不动战场尺度、不动玩法数值、不动 AI 行为,也不上线。

现在上线也看不出效果,原因有两个:游戏今天的距离尺度对应的光延迟只有零点几秒到两秒;而且 GM 模式 `adminMode` 默认是开的(`core/01-state.js` 第 46 行),默认画面本来就是真值。

| 距离 | 出处 | 光延迟 |
|---|---|---|
| `15 万 km` | DD 传感器量程、主炮射程 | `0.50 s` |
| `25 万 km` | CA 传感器量程 | `0.83 s` |
| `35 万 km` | 导弹射程 | `1.17 s` |
| `60 万 km` | ESM 探测范围(`sensors/21` 第 28 行) | `2.00 s` |

## 2. 设定定案(用户已拍板)

| 条目 | 定案 |
|---|---|
| 作用范围 | 光速延迟只作用于敌我之间的观测。 |
| 我方舰间通信 | 量子实时通信:任何一艘看见的,全舰同时看见,所有舰船是一体的。演示页左栏的「量子通信版本」开关用来和"舰间光速通信"对照,演示页默认是开关关、也就是光速通信。 |
| 光速通信下的前哨 | 前哨的报告要走"敌舰到前哨、再到主队"的折线,由三角不等式,永远不比光从敌舰直接到主队更早。所以前哨改善不了主队的态势图,也提供不了光束预警,只改善它自己的。设计推论:光速通信下应给前哨自主开火与规避的权限,它是分权的指挥节点;量子通信下前哨退化成全舰的一只眼,指挥可以集中。 |
| 命中告警 | 看见开火之后默认挂出命中倒计时,不需要任何开关,光束与炮弹都有。不画预测弹道的虚线,不画目标圈。 |
| 推算现位 | "弹道武器一旦被看见,此刻的真实位置就能精确推算"这个结论成立。但用户试过在观测层画推算圈、推算线和"预测"勾选框之后,决定整个撤回。不要再主动提议加回。 |
| 光束渐隐 | 渐隐做进脉冲剖面本身,不做命中后统一淡出;拖尾时间常数定为 `0.15`(先后试过 `0.45`、`0.30`);不加尘埃余辉。 |
| 真实层光束 | 画普通纯色虚线,不做渐变。 |
| 图层控件 | 保持「观测 \| 叠加 \| 真实」三键一组。 |
| 量子链路 | 量子通信下,我方每艘舰画一条连回主舰的浅色呼吸虚线,按墙钟呼吸,与时间倍率无关(演示页已做)。 |
| 设计结论 | 弹道武器一旦被看见,光速延迟的迷雾只剩两处:看见之前的那段时间,以及会机动的东西(敌舰、制导导弹)。前哨与机动能力是这套设计的核心。 |

还没定的几件事(战场尺度、双方通信是否对称、开关默认值等)列在第 9 节。

## 3. 物理模型

以下单位一律是 km 与 s,光速 $c = 299792.458$ km/s。"眼"指我方一艘舰:量子通信下全部我方舰都是眼,光速通信下态势图等价于只有主队一只眼。

### 3.1 推迟时刻

在时刻 $t$、位于 $P$ 的一只眼看到的目标,是满足 $|x(t_e) - P| = c\,(t - t_e)$ 的那一刻 $t_e$ 的样子,影像年龄 $\tau = t - t_e$。全舰态势图对每个目标取所有眼里 $t_e$ 最大、也就是最新的那一张。

### 3.2 舰船(会机动)

求解分两步。第一步按此刻距离筛眼:设舰速上限与光速之比为 $\beta$,推迟光程只可能落在 $d/(1+\beta)$ 到 $d/(1-\beta)$ 之间,因此比最近那只眼远出 $(1+\beta)/(1-\beta)$ 倍的眼不可能看得更新,可以直接跳过。这是严格的界,不是近似。第二步对留下的眼做不动点迭代 $t_e \leftarrow t - |x(t_e) - P|/c$,每走一步误差缩到原来的 $v/c$ 倍,两三步就到机器精度。演示页里它和逐眼 56 步二分逐时刻对照,敌舰机动中、4 只眼,最大差 `8.5e-14 s`,最早看见的那只眼一次都没选错。

这需要目标的航迹历史。演示页按 `0.5 s` 采样、线性插值(`histAt`);第一个采样之前按初始速度匀速反推。

可能位置圈:按影像的速度外推 $\tau$ 得到圆心,半径 $\tfrac12 a\tau^2$。这是加速度有界的直接推论,与敌舰怎么机动无关。自检里敌舰满推力掉头之后,真实位置始终在圈内,最大占比 `1.000`。

看起来是什么样:一段按时播放、但已经过期的录像,形状不畸变,因为 `560 km/s` 只有光速的 `0.19%`。在 1 光分外,影像比真实位置落后约 `3.4 万 km`,可能位置圈半径约 `2.7 万 km`;3 光分时圈半径约 `24 万 km`。离得近的敌舰影像更新,所以战场图其实是不同时刻拼起来的:特写里三艘敌舰的影像年龄分别是 `60.05 s`、`60.00 s`、`59.95 s`。

### 3.3 炮弹(匀速直线)

炮弹有解析解:推迟时刻满足一个二次方程。演示页把单位换成光秒以免大数相消,并在 $B \ge 0$ 时改用稳定形式(`shellTe`)。

设弹速 $u$、$\beta = u/c$,$\theta$ 为弹道方向与"弹指向观察者"方向的夹角,则视速度为 $u/(1-\beta\cos\theta)$,正对观察者时是 $u/(1-u/c)$;多普勒因子 $\delta = 1/(\gamma(1-\beta\cos\theta))$。从看见炮口闪光到命中的预警时间是 $D/u - D/c$。

10 光秒距离、正对主队的读数:

| 弹速 | 看见闪光时真弹已飞完全程的 | 视速度 | 预警时间 | 多普勒 $\delta$ |
|---|---|---|---|---|
| `0.1c`(游戏主炮,`CFG.macSpd=30000`) | `10%` | `0.11c` | `90 s` | `1.11` |
| `0.5c` | `50%` | `1.00c` | `10 s` | `1.73` |
| `0.9c` | `90%` | `9.00c` | `1.1 s` | `4.36` |
| 光束 | `100%` | 瞬间 | `0 s` | — |

游戏主炮这一档看起来和没有光速延迟几乎一样,只是出发晚了一拍;`0.9c` 的弹看上去以 9 倍光速扑来,更亮也更偏蓝。

### 3.4 光束

可见条件:对位于 $P$ 的眼,光束上离开火点 $s$ 处的那一点此刻看得见,当且仅当 $g(s) = s + |P(s) - P|$ 落在 $[c(t - t_0 - \text{span}),\ c(t - t_0)]$ 之内。$g$ 随 $s$ 单调不减,所以每只眼看到的是一整段,两端各二分一次即可。

多只眼时,光束逐点交给离它最近的那只眼:每只眼的沃罗诺伊胞与光束直线交成一段区间(`beamCells`),全舰所见是各段的并。分界点上两只眼同时看见,所以画面连续。

某只眼看到的前沿视速度是 $c/(1-\cos\varphi)$,$\varphi$ 是光束方向与"该点指向这只眼"方向的夹角。于是打向观察者本人的光束,闪光、光束、命中同时到达,整根同时亮起,零预警;打向别人的光束从敌舰影像处窜出,越接近目标越慢,演示里打侧翼那道的视速度从约 `3.4c` 降到约 `0.66c`。

"伸到一半突然接上"的原因:光束上离目标比离其他任何一只眼都近的那一截,只能由目标自己最先看见;而光束正冲着目标飞,那一截散射出来的光和光束前沿同路同速,所以那一截上每一点被看见的时刻都等于命中时刻。这段信息在命中之前根本不存在,画面只能在命中那一刻一次补齐。跳变点落在目标与另一只最近的眼之间的中垂线上。前哨在场、光束打侧翼时实测跳变点在全程 `43%` 处,侧翼的预警是 `42.4 s`,没有前哨时只有 `7.9 s`。这条规律对任何光速武器都成立:紧挨着目标的最后一小段,永远要到命中那一刻才看得见。

脉冲剖面 `beamI`:前沿陡起,平顶持续 $0.4 \cdot \text{dur}$,之后按 $\exp(-(f - 0.4)/0.15)$ 拖尾,亮度低于 `0.04` 算看不见;脉冲时间尺度 $\text{dur} = \mathrm{clamp}(0.12 \cdot D/c,\ 2,\ 36)$ s。画面上某一点的亮度取决于"它此刻被看见的那一刻,脉冲前沿已经过去多久",所以渐隐自带光速延迟:迎头看的那一段各点相位相同,整段同步变暗;侧面看的那一段头亮尾暗,像彗星。

| 距离 | 脉冲时长 `dur` | 从前沿到完全看不见 |
|---|---|---|
| 10 光秒 | `2.0 s` | `1.77 s` |
| 1 光分 | `7.2 s` | `6.36 s` |
| 3 光分 | `21.6 s` | `19.07 s` |

### 3.5 事件与告警

开火闪光、命中、敌舰机动这类事件,被全舰看见的时刻等于事件时刻加上"最近那只眼的距离除以 $c$"(`seeT`)。某艘舰自己最早何时知道:量子通信下就是全舰最早的那一刻,光速通信下是它自己看见的那一刻(`knowAt`)。

命中告警按弹道认目标:沿弹道方向第一艘挡在路上、离轴线 `1000 km` 以内的我方舰就是目标(`rayTarget`)。光束与无制导炮弹都不拐弯,所以认出目标之后,命中时刻等于开火时刻加路程除以速度,是精确值。预警时间统一口径为"命中时刻减去目标舰自己最早知道开火的时刻"。

### 3.6 两种通信设定的对照读数

1 光分、演示页场景④、前哨在场:

| 读数 | 光速通信 | 量子通信 |
|---|---|---|
| 态势图上的敌舰影像年龄 | `60.0 s`,来自主队 | `25.5 s`,来自前哨 |
| 前哨转报到主队时的影像年龄 | 约 `71 s`,比主队直接看到的还旧 | 全舰同步,无需转报 |
| 看见敌舰掉头 | `T+64 s` | `T+29.5 s` |
| 光束打主队的预警 | `0 s`,前哨的警报比命中晚到约 `11 s` | `34.5 s` |

没有前哨、光束打侧翼时,光速通信下侧翼被打中后 `49.5 s` 主队才看见;量子通信下侧翼有 `7.9 s` 预警,命中那一刻全舰就知道。把可拖动的机动舰放到敌舰近旁 `(0.1, 0.85) D`,量子通信下敌舰影像只旧 `10.8 s`,光速通信下仍是 `60 s`。

### 3.7 演示里理想化掉的部分

- 真空里光束本身看不见。画出来的线假设了星际尘埃散射而且亮度均匀,用户认可这个前提。迎头看的那只眼其实只会看到一个点突然亮起、分辨不出远近,演示把它画成摆在真实位置上的线,属于借助已知方向的重建。
- 量子实时通信是科幻设定(不可通信定理不允许靠纠缠传信息)。它隐含一个全宇宙统一的"同时",和游戏的全局时钟不矛盾。
- 探测是完美的:没有测量误差(方向差 1 µrad,在 1 光分处就偏约 `18 km`),弹体永远看得见,探测距离无限。
- 多普勒亮度按发光体算,没按反光的双重频移算。
- 演示是二维的;敌方瞄准用的是真实位置,因为演示里我方静止。

### 3.8 设计张力(定尺度时用)

- 光束的瞄准误差来自 $2\tau$ 这段时间里目标的机动,误差半径 $\tfrac12 a (2\tau)^2$。按命中判定半径 `2000 km`(`weapons/56` 第 56 行)和 CA 推力 `15 km/s²` 算,约 8 光秒以外,满推力规避的 CA 就打不中了;1 光分时误差约 `10.8 万 km`。
- 主炮 `0.1c`:看到的影像已经延迟 $\tau$,炮弹再飞 $10\tau$,误差 $\tfrac12 a (11\tau)^2$ 超过 `2000 km` 的临界约 1.5 光秒(`45 万 km`)。今天的主炮射程 `15 万 km` 远在这个临界点以内。
- 因此"光束零预警、但远了打不中;炮弹打得远、但躲得开"很可能就是这套设计的核心取舍。
- 离敌舰最近的那艘船,既是全舰的眼,也是唯一吃零预警光束的位置。场景④里主队靠前哨换来 `34.5 s` 预警,按 CA 推力足够横移约 `8900 km`,超过命中判定半径,所以前哨会成为双方都优先要打掉的目标。

## 4. 演示页 `光速延迟.html`

演示页在仓库根目录,是独立页面,不接进游戏,也没有提交。双击打开会自动播放场景①。

| 场景 | 距离 | 内容 |
|---|---|---|
| ① 敌舰机动 | 1 光分 | `T+8 s` 敌舰掉头 |
| ② 炮弹来袭 | 10 光秒 | `T+3 s` 四种弹速同时打主队 |
| ③ 光束来袭 | 1 光分 | `T+4 s` 同时打主队和侧翼 |
| ④ 前哨预警 | 1 光分 | 前哨在场,`T+4 s` 掉头,`T+30 s` 光束打主队 |

启动参数 `?scene=n&t=秒&layer=obs|mix|true&quantum&rover=fx,fy` 可以直接跳到某一刻,截图用;`?selftest` 跑物理自检,`?uitest` 跑交互自检,交互自检全部走真实的 click、input、pointer 事件。2026-09-11 在无头 Chrome 里重跑,物理自检 `36` 条、交互自检 `28` 条全部通过,标题分别是 `SELFTEST:PASS` 与 `UITEST:PASS`。第 28 条是同日补的回归判据:时空图 `drawST` 的采样函数 `samp` 原先按 $a+(b-a)\cdot n/n$ 算最后一个点,舍入会让它比终点 $b$ 多一个 ulp;炮弹命中之后 `shellTrue` 在那一刻返回 `null`,`rOf(null)` 抛错,`frame()` 走不到末尾的 `requestAnimationFrame`,整页永久卡住(用户实报"炮弹来袭演示到一半卡住",实测卡在 `T+18` 到 `T+24 s` 之间的随机时刻)。现在终点直接取 $b$。移植时同样的采样写法要照此处理。

哪些函数要搬、搬到哪一层:

| 函数 | 作用 | 去向 |
|---|---|---|
| `shipRet(i,t)` | 舰船影像:按距离筛眼加不动点迭代 | L1 光锥 |
| `histAt(t)` | 航迹插值,第一个采样之前匀速反推 | L1,改成环形缓冲 |
| `shellTe` / `shellObs` | 炮弹推迟时刻的解析解 / 全舰所见 | L1 |
| `seeT` / `knowAt` | 事件何时被全舰看见 / 某舰何时知道 | L1 |
| `drOf(g)` | 外推点与可能位置圈 | L3 态势 |
| `rayTarget` | 按弹道认目标、算命中倒计时 | L3 命中告警 |
| `beamG` `beamInv` `beamCells` `beamObs` `beamI` `beamSpan` `beamFrontSpeed` | 光束影像与脉冲剖面 | 游戏里还没有光束武器,先不搬 |
| `eyes` / `nearestEye` | 态势图由哪几只眼拼成 | 游戏里用 `detectorsOf(side)` 代替 |
| 绘制、UI、`frBuild`、拖动 | 演示专用 | 不搬 |

关键常量:

| 常量 | 值 | 含义 |
|---|---|---|
| `C` | `299792.458` | 光速 km/s |
| `EN` | `a:15, vmax:700, v0:560` | 敌旗舰按 CA:推力、最高档、巡航取八成 |
| `HREC` | `0.5` | 航迹采样间隔 s |
| `RET_BETA` | `EN.vmax/C` | 筛眼界里的 $\beta$ |
| `BEAM_FLAT` / `BEAM_TAIL` / `BEAM_CUT` | `0.4` / `0.15` / `0.04` | 平顶比例 / 拖尾时间常数 / 看不见的亮度阈值 |
| `LBL_MAX` / `RING_MERGE` / `RING_LBL` | `32` / `3` / `4` | 每视图标注上限 / 光环合并阈值 px / 光环标注个数 |
| `LINK_PERIOD` / `LINK_A0` / `LINK_A1` | `3.2` / `0.10` / `0.10` | 量子链路呼吸周期 s / 最暗透明度 / 呼吸幅度 |

两条移植注意:

- 光束分区缓存靠"舰船名单 `S.fr` 的引用有没有换"判断眼变没变,所以名单只许整体替换、不许原地修改。游戏里对应的缓存要找一个同样可靠的失效信号。
- 演示用了一批很短的全局名(`C`、`S`、`U`、`EN`、`FORM`、`COL`、`hyp`、`dist`)。2026-09-11 grep 确认游戏里目前没有同名顶层声明,但照搬必然埋雷(RF3 撞名教训),移植时一律换成带前缀的名字;二维的 `hyp` 改成三维的 `V.len`。

## 5. 性能

演示页实测,条件是无头 Chrome、软件渲染。前后对比用的是同一台机器、同一份基准脚本(PERF1 优化):

| 项目 | 优化前 | 优化后 |
|---|---|---|
| 典型场景整帧(场景③ `T+66`) | `794 µs` | `239 µs` |
| 典型场景的光速延迟计算 | `18.3 µs` | `1.6 µs` |
| 压力场景整帧(4 眼、20 光束、50 炮弹、140 光环) | `8.0 ms` | `4.6 ms` |
| 压力场景的光速延迟计算 | `103 µs` | `28 µs` |
| 单艘敌舰影像,4 只眼 | `9.8 µs` | `0.33 µs` |
| 单艘敌舰影像,64 只眼 | `194 µs` | `1.4 µs` |
| 单道光束影像,64 只眼 | `24.5 µs` | `0.1 µs` |

规模上的规律:舰船与制导导弹的开销随"物体数乘眼数"线性增长,因为它们会机动,要按航迹历史求推迟时刻;炮弹有解析解,几乎不花时间;光束多一项"眼数的平方",来自给每只眼划分负责区段,好在同时在飞的光束不会多。

接进游戏的估算:按每方每个接触约 `1 µs` 算,100 艘船、两方,一个 tick 约 `0.2–0.3 ms`。但时间倍率拉满时一帧最多跑 100 个 tick,合计可达 `20–30 ms`,所以态势图不能每个 tick 全量重算,要么降频(例如每 `0.1 s` 模拟时间一次),要么谁用谁算。航迹历史按 `0.1 s` 采样、每个采样约 `64 B` 估算:1 光分尺度下约 600 个采样,每艘船约 `38 KB`;今天的尺度(最长 `2 s`)只要约 20 个采样。船大部分时间在匀速巡航,只在加速度变化时存采样、中间用解析公式补,能再小一个数量级。事件(闪光、命中)应在发生时就算好到达时刻,放进按到达时刻排序的队列,到时间再取出。

这套机制真正的成本不在 CPU,而在代码组织:AI、火控、界面每一处做决策的地方,都得改成读本方的态势图,而且敌我各一张。

## 6. 接入架构

原则一句话:决策只读态势图,物理只认真值。

| 层 | 职责 | 读 | 写 | 放在哪 |
|---|---|---|---|---|
| L0 真值 | 运动、碰撞、伤害 | — | `ships` / `projectiles` | 现有代码,不动 |
| L1 光锥 | 记录航迹历史;回答"某只眼此刻收到的是目标哪一刻的光";各类事件何时到达各方 | L0 | 航迹缓冲、事件队列 | 新建 `sensors/22-lightcone.js` |
| L2 探测 | 信号够不够强、接触等级 | L1 的推迟状态 | `trk` / `lit` | 改造 `sensors/21-detect.js` |
| L3 态势 | 每方一张图:影像、年龄、外推、可能位置圈、命中告警 | L1 与 L2 | 每方的接触表 | 以后新建 `sensors/23-picture.js` |
| L4 决策与显示 | AI、火控、界面 | 只读 L3;GM 模式读 L0 | 命令 | bots / weapons / command / render |

几点说明:

- 放进 `sensors/` 而不是另起 `fog/`,因为它本来就是感知系统的底座,也符合"目录即系统、十位是层号"的约定。
- GM 模式(F8)正好对应演示页的"真实"图层,普通模式对应"观测"图层,这层对应关系是现成的。
- 一个好消息:`detectorsOf(side)`(`sensors/21` 第 4–8 行)已经把一方所有活着的舰和开着的信标当成一个网络,取最强的那一路信号,这正是量子通信"全舰一体"的模型。光锥层可以直接垫在它下面,不需要重构探测架构。反过来,如果以后要做"某一方只有光速通信",就得每只眼单独算、再按通信规则合并,那是另一个量级的改动。
- `stepSim` 里的插入点:航迹历史紧跟在 S4 运动结算之后记录;态势图在每个 tick 开头统一刷新一次,保证同一拍里所有决策读到的是同一张图。S3b 必须早于 S4 这类既有硬约束不受影响。
- 目标的对象引用不改结构:`lockedTarget`、`fcTgt`、任务目标现在存的都是真实舰船对象。决策路径改走取数函数,命中结算照旧对真实舰船。这样改动面最小,也方便 grep 查漏:决策代码里只要还直接读敌舰的 `.pos`,就是没改完。
- 航迹历史必须连同发射相关的状态一起记:位置、速度,以及决定信号强度的 `flame`、`sideFlame`、`lidar`、`ecm`、`dead`。只记位置的话,探测会拿"此刻的引擎状态"去配"过去的位置"。
- LADAR 是主动照射,接入后要按来回两趟算,火控级航迹因此会比现在晚一拍。
- 导弹导引头本身就是一只眼(DS147 的口径是"导弹只知道自己看到什么")。`weapons/54` 的 `missSee` 目前用真实距离和目标此刻的信号;导弹与目标的距离通常在 `15 万 km` 以内,光延迟不到半秒,可以排在最后接。

## 7. 游戏现状盘点

以下核对于 2026-09-11,提交 `806b68d`;在 `6a825a3` 上复核,引用的文件未变。

### 7.1 感知链路现在怎么走

`stepSim` 的 S1 每秒调一次 `detectLoop`(`core/05` 第 9 行)。它对两方各跑一次 `detectFor`,按目标此刻的位置和引擎状态算红外、ESM、LADAR 三路通量,积分成 `trk` 三通道驻留,再派生出接触等级 `lit`。当红外或 LADAR 通量超过探测下限时,它把目标此刻的真实位置和速度复制进 `seen*Pos` / `seen*Vel`(`sensors/21` 第 88–90 行)。

`contactState` 按"是否点亮"和"信息年龄"分档:点亮且年龄不超过 `5 s` 是实况,点亮但更旧是陈旧,未点亮但曾经点亮且年龄不超过 `30 s` 是幽灵,其余不显示。`render/82` 的 `drawShip` 只在非 GM、非编辑模式下对红方舰按档绘制:实况画真实位置;陈旧和幽灵画"最后已知位置加上速度乘年龄"的外推位置,再加一个不确定圈,半径是速度乘年龄,夹在 `8000–200000 km` 之间。

另外,`projVisibleTo` 每秒把弹丸对两方的可见性缓存进 `visBlue` / `visRed`;`updateESMFixes` 只做蓝方看红方,给 ESM 接触画不确定椭圆;`ladWarned` 在我方舰被敌方 LADAR 照射时即时告警。

### 7.2 感知字段

| 字段 | 声明 | 写 | 读 | 结论 |
|---|---|---|---|---|
| `litBlue` / `litRed` | `ships/11` | `sensors/21` `detectFor` | 49 处 / 20 个文件 | 保留;以后由推迟状态驱动 |
| `trkB` / `trkR` | `sensors/21` 惰性创建 | `sensors/21` | 8 处 / 5 个文件 | 保留 |
| `seenBlue` `seenBluePos` `seenBlueVel` | `ships/11` 第 127 行 | `sensors/21` 第 89 行 | `contactAge`(`sensors/21` 第 112 行)、`render/82` 第 48、59 行 | 并入接触记录(A1) |
| `seenRed` `seenRedPos` `seenRedVel` | `ships/11` 第 127 行 | `sensors/21` 第 89 行 | 无 | 只写不读 |
| `everLitBlue` | `ships/11` 第 126 行 | `sensors/21` 第 100 行 | `contactState`(第 118 行) | 并入接触记录 |
| `everLitRed` | `ships/11` 第 126 行 | `sensors/21` 第 100 行 | 只在 `contactState(…,'red')` 里读,而全库只有 `'blue'` 一种调用 | 实际上是死的 |
| `visBlue` / `visRed` | 无声明,`sensors/21` 第 21 行写 | `sensors/21` | 连同 `projVisibleTo` 共 13 处 / 6 个文件 | 保留 |
| `ladWarned` | 无声明 | `sensors/21` 第 16–17 行 | `sensors/21` | 即时告警,上线阶段处理 |

`js/` 以外,只有自动生成的 `tools/.syms.txt` 出现了 `contactAge` 与 `contactState` 两个函数名;`tools/verify.sh` 没有引用任何一个 `seen*` 或 `everLit*` 字段。

### 7.3 直接读敌方真实状态的地方

| 位置 | 读了什么真值 | 本计划怎么处理 |
|---|---|---|
| `sensors/21` `detectFor`(第 72–89 行) | 目标此刻的位置与引擎状态;`seen*` 直接复制真实位置和速度 | A3:收进 `emitterState`,暂时仍返回真值 |
| `sensors/21` `projVisibleTo`(第 124–137 行)、`updateESMFixes`(第 23–58 行) | 真实距离、真实位置三角定位 | A3:同上 |
| `render/82` `drawShip`(第 41–66 行) | 实况接触画真实位置;外推公式直接写在绘制函数里 | A2:外推搬进 `contactPos`,绘制只取数 |
| `command/70` `targetAt`(第 94–104 行) | 准星吸附与右键指定目标的命中测试按真实位置 | A2 |
| `command/74` `xhCardHTML`(第 107–109 行) | 信息卡上的距离与方位 | A2 |
| `command/74` 第 255 行、`render/89` 第 198 行 | 轮盘锚点、轮盘到目标的引线 | A2 |
| `render/83` 第 84、443、568 行 | 锁定线、准星预览线的端点、火控数据链的目标节点 | A2 |
| `render/88` 第 367 行 | 右栏的目标距离读数 | A2 |
| `command/74` 第 195 行、`render/89` 第 97 行、`render/83` 第 447 行 | 轮盘扇区与预览线的"射程内外"判定 | 本轮不动,上线阶段与 `fcGate` 一起搬(理由见 8.1 的 A2) |
| `weapons/52` `macPred` / `macAligned`(第 3–8 行)、`weapons/58` `fcGate`(第 163–180 行)、`weapons/57` 自动索敌(第 25、40 行)、`weapons/54` `missSee` 与 `guideSide`、`weapons/53` 网分配、`weapons/56` 拦截弹选靶 | 真实位置、速度、距离 | 上线阶段 |
| `bots/61` `enemyAI`(第 20–21 行挑最近目标,第 35 行规避)、`bots/60` 任务、`scenario/95` 靶场 AI | 按真实位置决策 | 上线阶段 |
| 命中闪光 `hitFX`(`weapons/52` 第 49 行生成,`render/83` 第 93 行起无门控绘制)、被照射告警 `ladWarned`(`sensors/21` 第 12–18 行) | 对所有人即时可见 | 上线阶段:改成按事件到达时刻显示 |
| 敌方开火日志(`weapons/52` 第 45、98、178 行) | 普通模式下整条隐藏,GM 模式下即时显示 | 上线阶段:可改成"看见炮口闪光时才出日志" |

### 7.4 一处现成的漏真值

对陈旧接触(仍点亮、但超过 `5 s` 没刷新),舰船图标画在外推位置,而准星命中测试 `targetAt`、信息卡的距离与方位、右栏目标距离都按真实位置算。结果是把鼠标移到"看不见的地方",准星照样会吸到那艘真船,信息卡还会报出真实距离。幽灵接触因为 `lit` 已经归零,`targetAt` 直接跳过它,所以不漏,但也无法被指定为目标。以后接入光锥层,这个漏洞会原样跟过去,所以放在阶段 A 先修。

### 7.5 规模数据(可复现的 grep 模式)

| 模式 | 出现次数 | 文件数 |
|---|---|---|
| `lockedTarget` | `49` | `18` |
| `(lockedTarget\|target\|tgt\|\bt)\.(pos\|vel)\b`(粗计读目标位置或速度,含 `formation/41-follow` 这类友方误报) | `68` | `15` |
| `litBlue\|litRed` | `49` | `20` |
| `trkB\|trkR` | `8` | `5` |
| `projVisibleTo\|visBlue\|visRed` | `13` | `6` |

上一版计划写的"约 69 处、16 个文件"没留下所用的模式,这里换成可复现的模式重新数。

### 7.6 与接入无关的死代码

盘点时顺带看到的三处死代码与光速延迟无关,2026-09-19 迁到仓库根 `todo-plan.md` 第 5 节。

## 8. 计划

前提:新开关 `CFG.lightLag` 默认 `false`,关闭时游戏行为逐位不变。每一步的验证都是三件:`tools/verify.sh` 全部通过,且 `SYMS_MISSING` 与 `THREW` 为 `none`;运动基准 `node tools/train/bench_all.js` 仍是 `5.8607`;新增判定加进探针底部的总判定段(RF12 教训:不加的话这条判定永远不会变红)。另一个会话在同时改编队相关的文件,动手前先看 `git status` 和 `git log`;本计划的改动面与编队文件不重叠,只有 `index.html` 和 `tools/verify.sh` 是共用的。

### 8.1 阶段 A:清理(提交标记建议 `LL0`)

除 A2 外,行为零改变。

- A1 收拢接触记录。把散在每艘舰上的 8 个字段(`seenBlue`、`seenBluePos`、`seenBlueVel`、`seenRed`、`seenRedPos`、`seenRedVel`、`everLitBlue`、`everLitRed`)收成一个对象 `s.contact = {blue:{t,pos,vel,ever}, red:{t,pos,vel,ever}}`。改动点有:`ships/11` 的 `makeShip` 初始化(第 126–127 行);`sensors/21` 的 `detectFor`(第 64–67 行的键名、第 89 行、第 100 行);`contactAge` 与 `contactState`;`render/82` 第 48、59 行。红方那一份照旧保留,因为以后敌方 AI 要读自己的态势图。
- A2 统一"敌舰显示在哪儿"的取数口。在 `sensors/21` 新增 `contactPos(ship, side)`,把 `render/82` 里的外推公式搬过来:GM 或编辑模式返回真值;实况接触返回真实位置(今天如此,接入后换成推迟影像);陈旧与幽灵返回外推位置。然后让纯显示和命中测试的地方都走它:`render/82` 的绘制、`command/70` 的 `targetAt`、`command/74` 的信息卡与轮盘锚点、`render/89` 的引线、`render/83` 的锁定线与预览线端点和数据链节点、`render/88` 的距离读数。对实况接触它返回的就是真实位置,所以画面不变;变化只发生在陈旧接触上,准星会吸到图标上,而不是吸到那艘看不见的真船上。
  三处"射程内外"判定(`command/74` 第 195 行、`render/89` 第 97 行、`render/83` 第 447 行)这一轮不搬。RF5 与 RF6 备忘要求它们和 `fcGate` 逐条同口径;只搬显示、不搬 `fcGate`,会重新造出"轮盘说能打、引擎不开火"的矛盾。代价是陈旧接触的射程判定在上线前仍按真实距离算;等 `fcGate` 在上线阶段改读态势图时,四处一起搬。
- A3 给探测里的真值直读加一层。新增 `emitterState(t, eye)`,返回目标的位置、速度和 `flame`、`sideFlame`、`lidar`、`ecm`,目前原样返回真值;`detectFor`、`projVisibleTo`、`updateESMFixes` 全部改走它。这一步纯属重构,作用是把光锥层以后的插入点精确标出来。
- A4 已迁出:那两处死代码与光速延迟无关,见 `todo-plan.md` 第 5 节。
- 验证:除上面三件,探针再加一条静态检查,确认 8 个旧字段名在 `js/` 下搜不到;A2 加一条走真实鼠标事件的判定,人为造一个陈旧接触,断言准星吸附点等于图标位置而不是真实位置,并做反向对照(改回真实位置必须转红)。

### 8.2 阶段 B:放进光锥层,默认关闭(提交标记建议 `LL1`)

行为零改变。

- B1 新建 `js/sensors/22-lightcone.js`,从演示页移植纯函数:航迹环形缓冲(连同第 6 节提到的发射状态);按距离筛眼加不动点迭代求推迟时刻,距离改用三维 `V.len`;炮弹影像的解析解;事件何时被看见。缓冲覆盖不到所需的推迟时刻时,照演示页的做法按最早采样的速度匀速反推,开关中途打开时会遇到这种情况。光束武器游戏里还没有,先不搬。
- B2 挂钩点:航迹记录挂在 `core/05` 的 `stepSim` 里、S4 运动结算之后,用 `typeof` 守卫,并且只在开关打开时记录,关闭时零开销。挂在这里而不是 `physics/31`,是因为 Node 训练环境 `tools/train/env.js`(第 8–10 行的 `FILES`)只加载 core/00、ships/10–11、sensors/20、weapons/51 的两个文件、physics/30–31 和 formation/40–44,不加载 `core/05`、`sensors/21` 和新的 `22`,运动基准因此完全不受影响。
- B3 把 A 阶段的两个取数口接上:`emitterState` 与 `contactPos` 按 `CFG.lightLag` 分叉,关闭时走现在的逻辑,打开时走光锥层。
- B4 配置:`core/00` 的 `CFG` 加 `lightLag:false`、航迹采样间隔 `0.1 s`、历史保留时长。
- B5 探针:把演示页的物理自检移植成一条新的 FLOW(清单见附录 A),探针内部临时打开开关、测完还原;另加一条性能预算:100 艘船、两方,一个 tick 的光锥计算不超过预算(建议 `0.5 ms`,动手时按实测定)。已有的全部探针在开关关闭的状态下跑,结果必须不变。
- `index.html` 在 sensors 段(目前第 251–252 行)之后加 `22-lightcone.js`,发版前统一改 `?v=`。

### 8.3 新名字(2026-09-11 grep 确认未被占用)

| 名字 | 用途 | 说明 |
|---|---|---|
| `CFG.lightLag` | 总开关 | |
| `js/sensors/22-lightcone.js` | L1 光锥 | |
| `s.contact` | 每方一份接触记录 | 原计划的 `s.ct` 不用:`ct` 已在 `ships/11` 第 59 行、`render/87-fmbar` 第 113–126 行做局部名,以后 grep 会很吵 |
| `contactPos(ship, side)` | 敌舰显示位置 | |
| `emitterState(t, eye)` | 探测读目标状态的唯一入口 | |
| `sightOf(ship, side)` | 上线阶段决策路径的取数函数 | 原先设想的 `seen(side, ship)` 不用:`seen` 已在 `tools/verify.sh` 第 92、766、2402、2903 行和 `weapons/53`、`weapons/56` 里做局部名 |

### 8.4 文件改动清单

| 阶段 | 新建 | 修改 |
|---|---|---|
| A | — | `ships/11-classes.js`、`sensors/21-detect.js`、`render/82-ship-icons.js`、`command/70-input.js`、`command/74-targeting.js`、`render/83-hud.js`、`render/88-selpanel.js`、`render/89-radial.js`、`tools/verify.sh`;A4 若做,再加 `weapons/51-defs.js`、`weapons/51-ciws.js`、`scenario/92-editor.js` |
| B | `sensors/22-lightcone.js` | `core/00-config.js`、`core/05-sim.js`、`index.html`、`tools/verify.sh` |

两个阶段都超过 3 个文件,按全局约定先给任务分解,等用户确认再动手。

### 8.5 不在这一轮(上线路线)

1. 显示改读推迟影像:实况接触也画"光到达时的影像",陈旧、幽灵两档并进同一套(影像年龄加可能位置圈);届时新建 `sensors/23-picture.js`,`contactPos` 搬过去。
2. 探测改用推迟状态,LADAR 按来回两趟算。
3. 武器与火控改读态势图:`macPred`、`fcGate`、自动索敌、导弹发射解算,以及三处射程判定一起搬;加上命中倒计时。
4. AI 改读态势图。这一步必须做:玩家看的是过去,AI 却看真值,就等于 AI 作弊。
5. 命中闪光、被照射告警、敌方开火日志改成按事件到达时刻显示,事件用按到达时刻排序的队列。
6. 定战场尺度,连带射程、传感器量程、航速、时间倍率。
7. 通信设定(是否允许不对称)、前哨玩法、光束武器。
8. 同步教程 `TUT_HTML`:它是手写的事实,不会跟着代码自动更新。

### 8.6 交付方式

按项目惯例一个阶段一个提交,标记建议叫 `LL0`(清理)与 `LL1`(光锥层),行内注释按 `LL0:` / `LL1:` 加标记与一句原因。每个阶段做完,同步更新 `CLAUDE.md`:文件地图的 sensors 一行、全局状态归属表,以及一节备忘。A2 改了陈旧接触的准星行为,做完要查一遍教程里有没有写到相关的吸附规则。

## 9. 待拍板

> 原第 3 行(删不删死代码)、第 9 行(接触列表面板先不先做)、第 10 行(要不要第四条光学通道)与光速延迟无关,2026-09-19 迁到 `todo-plan.md`。行号保持原样,所以下表缺 3 / 9 / 10。

| # | 问题 | 选项 | 建议 |
|---|---|---|---|
| 1 | A1 怎么收拢 | 红蓝两份合成一个接触记录对象 / 只删红方那份只写不读的字段 | 合成对象:以后敌方 AI 要读自己的态势图,删了还得加回来 |
| 2 | A2 准星修复 | 修 / 不修 | 修。这是本轮唯一可见的行为变化,只影响陈旧接触;射程判定按 8.1 留到上线阶段与 `fcGate` 一起搬 |
| 4 | 提交标记与记录策略 | `LL0` / `LL1`;航迹只在开关打开时记录 | 照此办 |
| 5 | 战场尺度 | 保持现状(光延迟 0.5–2 s)/ 扩大到光秒 / 扩大到光分 | 先把机制做成与尺度无关,接进去看过效果再定 |
| 6 | 双方通信设定 | 双方都是量子通信(每方一张图)/ 允许不对称(例如敌方只有光速通信) | 先双方量子;不对称要每只眼单独算再合并,另起一轮 |
| 7 | 开关默认值 | 默认关直到上线 / 现在就默认开 | 默认关,已有几十条回归探针不用改 |
| 8 | **主菜是哪一条路** | 电子战对抗:尺度不变,核心操作是辐射管理与欺骗 / 光速延迟下的战役指挥:尺度拉到光分,核心是情报滞后与前出部署 | 见第 11 节,优先级表在 `todo-plan.md` 1.8。两条路前期工作重合度很高(都要接触表、都要读图的 AI),但在优先级表第 4 条之后分叉,分叉之后很难回头。我的判断是先走电子战对抗,理由见 11.7 |

## 10. 风险与注意

- 运动基准的护栏:航迹记录绝不能放进 `physics/31` 或 `sensors/20`,否则 Node 训练环境会加载到它,`5.8607` 可能漂移。
- 距离一律用三维 `V.len(V.sub(...))`,不写平面 `Math.hypot`(RF5 教训:有 z 差的场景里,平面距离会在射程边界上给出相反结论)。演示页是二维的,移植时逐处改。
- 改"玩家能看到什么"的逻辑必须同时考虑 GM 分支,而 GM 默认是开的。
- 新增顶层 `const` 之前先 grep 同名(RF3 教训:跨 script 重复声明会让整个文件语法报废)。
- 跨文件调用一律 `typeof` 守卫,新文件加载早于或晚于依赖方都不能崩。
- 新 FLOW 要进底部总判定段,并做反向对照(RF12、RF22b 教训)。A2 的判定必须走真实鼠标事件,只调 `contactPos` 测不到接线错误。
- 高倍速下一帧最多 100 个 tick,光锥计算必须降频或按需计算。
- 另一个会话在同时改仓库,提交前确认工作树里只有本计划的改动。

## 11. 光速延迟在游戏里该占什么位置(2026-09-14;2026-09-19 整理)

> 这一节原来是"游戏化的战争迷雾"(11.1~11.11):整套迷雾的玩法框架、优先级表、对热区开火、好玩第一 …… 那些不是光速延迟的内容,
> 2026-09-19 迁到了仓库根 `todo-plan.md` 的第 1 节(小节标题后面括号里注着原编号)。这里只留与光速延迟直接有关的两段:原 11.7 与量级实测。
> 本文件别处提到的"11.8 / 11.9"现在在 `todo-plan.md` 1.8 / 1.9。

诚实的判断:当前尺度下光延迟是 `0.5`~`2 s`,玩家察觉不到。要让它成为主菜,尺度得扩到光分级,而那会把战术交战拉成几分钟一个来回,对这个已经调优了十几轮运动内核的实时战术游戏来说是**换一个品类**。

所以建议是**当调味和当地基,不当主菜**。它现在就能免费交付三样与尺度无关的东西:看见炮口闪光后挂出的**命中倒计时**(纯戏剧性,一个 UI 元素,玩家立刻理解);**可能位置圈 `½aτ²`** 作为"我有多不了解它"的统一视觉语言,它同时还是数据关联问题的严格物理门限;以及**前哨的价值有了物理根据**,不再是拍脑袋的数值。

由此引出第 9 节新增的第 8 条待拍板:主菜是**电子战对抗**(尺度不变,核心操作是辐射管理与欺骗),还是**光速延迟下的战役指挥**(尺度拉到光分,核心是情报滞后与前出部署)。两条路在优先级表第 4 条之前完全重合,之后分叉且难回头。我的建议是先走电子战对抗:它的骨架已经在数值里(11.2),不需要改尺度、不需要重做运动与射程的全部平衡,而且它做完之后光速延迟仍然可以整体加上去;反过来先扩尺度,现有的交战数值要全部重标定一遍,而那时电子战还没有玩法验证。

**光速延迟在 200 万公里的战场上有多大(2026-09-18 量过,记下免得再争):**`态势感知V3.html` 的无量纲比 P10 = 0.0097、P11 = 10。已有航迹的目标按匀速外推,光走过来的那点时间里它靠【加速】能偏出去的只有 ½·a·τ²:战场最远端(120 万公里,τ = 4 秒)也才 160 公里,而同一距离上的误差椭圆是几万公里;主炮射程上是 2.5 公里对 1400 公里的火控门。所以在战术层它**只做标注**(接触卡上的「情报龄」= 被动单程 d/c、照射往返 2d/c,海军叫 time late),不进椭圆、不改任何一级距离。真正有分量的"你看到的是过去"是**弹道时间**(弹速 0.1c,是光延迟的 10 倍)—— 本计划阶段 B 的弹道提前量才是它在战术层的落点。光速延迟要当【机制】,属于比这大一个数量级的那一层(战略 / 跨战场通信),不是靠把战场放大来硬凑。详见 `态势感知的问题.md` 8.2。

## 附录 A:演示页物理自检清单(B5 移植参照)

演示页 `runSelftest` 共 36 条:舰船 6 条、舰船与通信设定 3 条、炮弹 8 条、光束 17 条、光束分区缓存 1 条、光环绘制 1 条。游戏里还没有光束武器,也没有演示页的绘制层,所以 B5 先移植舰船与炮弹那 14 条,通信设定那 3 条等以后做不对称通信时再移植,其余 19 条等游戏有光束武器时再说。

| 演示页判定 | 移植后的形状 |
|---|---|
| 静止敌舰:影像年龄等于最近那只眼的光程(侧翼更远,仍是主队的 $D/c$) | 静止目标的影像年龄等于到最近一只眼的距离除以 $c$ |
| 前哨在场:敌舰影像年龄等于前哨到敌舰的光程 | 多只眼时取最近的那只 |
| 机动舰加入观测网:影像来自离敌最近的它 | 新增一艘靠前的我方舰,影像年龄随之变短 |
| PERF1:筛眼加不动点迭代与逐眼二分一致(机动中,4 只眼) | 逐时刻对照,最大差小于 `1e-9 s`,选中的眼一致 |
| 匀速航行时外推误差小于 `5 km` | 同 |
| 掉头后真实位置始终在可能位置圈内 | 同,最大占比不超过 1 |
| 看见闪光之前没有弹影 | 同 |
| `0.5c` 正对主队,视速度等于 `1c` | 视速度等于 $u/(1-u/c)$ |
| 弹影落在看见它那只眼的"此刻所见"上 | 同 |
| 命中前 `1 ms` 弹影离主队约 `300 km` | 同 |
| 预警时间等于 $D/u - D/c$ | 同 |
| `0.9c` 正对:多普勒约 `4.359`,视速度约 `9c`(两条) | 同 |
| 炮弹命中告警:只凭方向认出目标,推算命中时刻与真实一致 | 同 |
| 光速通信下前哨与机动舰改善不了主队的画面,转报比直接看到的更旧(三条) | 仅当以后做不对称通信时移植 |

另加两条游戏专有的判定:开关关闭时,全部已有探针结果不变;性能预算。
